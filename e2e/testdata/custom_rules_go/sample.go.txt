package sample

// Let's dive into the configuration options. // want "company.no-dive-in"
const message = "Let's \u0064ive into the configuration options." // want "company.no-dive-in"

// The configuration file defines connection options.
const clean = "The configuration file defines connection options."
