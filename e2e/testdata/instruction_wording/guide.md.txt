It is possible to display the **café** label. This is done by using the label option. <!-- want "filler.instruction-scaffolding" -->

If you want to customize the dashboard, you can select a theme. <!-- want "filler.instruction-scaffolding" -->

The default path for the configuration file is located at `/etc/example.conf`. <!-- want "repetition.redundant-predicate" -->

It is possible to lose data during a failed write.

It is possible to configure the queue only when the worker is stopped.

The configuration file is located at `/etc/example.conf`.

The example says "It is possible to configure the queue".

It is `possible to configure` the queue.
