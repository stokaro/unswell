package sample

// The client may retry the failed request after the server closes the connection and releases all resources.
func Retry() {}

const Contract = "The client must retry the failed request after the server closes the connection and releases all resources."

const First = "The adapter opens a connection to the server and sends the reque\u0073t with its credentials." // want "repetition.near-sentence"
const Second = "The adapter creates a connection to the server and sends the request with its credentials."

const Label = "Short text."
