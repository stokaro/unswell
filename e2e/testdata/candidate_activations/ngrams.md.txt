The clear **release** notes explain the release schedule for every new reader. <!-- want "repetition.ngram-density" -->

Our clear release notes describe the maintenance window for the new team.

These clear release notes document the support process for each new user.

The client opens a connection to the server and sends the request with its credentials.

The client opens a connection to the server and sends the request with its `credentials`.

Short text.
