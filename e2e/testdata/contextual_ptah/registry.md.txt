**Implementation status.** Out of scope. Login, the Atlas Registry, the Cloud UI, monitoring, and the Cloud APIs are account-bound services rather than Atlas OSS drop-in targets. <!-- want "syntax.paired-contrast-density" -->

Out of scope is the hosted service, not the capability: Ptah publishes and promotes through any [OCI registry](../../operate/oci-registry/).
