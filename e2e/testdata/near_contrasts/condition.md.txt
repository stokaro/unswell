The client may retry the failed request before the server closes the connection and releases all resources.

The client may retry the failed request after the server closes the connection and releases all resources.
