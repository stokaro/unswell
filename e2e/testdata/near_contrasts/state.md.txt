The client uses enabled settings to retry the failed request after the server closes the connection and releases all resources.

The client uses disabled settings to retry the failed request after the server closes the connection and releases all resources.
