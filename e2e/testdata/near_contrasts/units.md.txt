The client waits 30 seconds to retry the failed request after the server closes the connection and releases all resources.

The client waits 30 minutes to retry the failed request after the server closes the connection and releases all resources.
