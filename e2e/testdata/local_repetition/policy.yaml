version: 1
extends: [builtin:custom]
rules:
  repetition.adjacent-word: {enabled: true, gate: forbid}
  repetition.repeated-claim: {enabled: true, gate: forbid}
  repetition.explanatory-restart: {enabled: true, gate: forbid}
