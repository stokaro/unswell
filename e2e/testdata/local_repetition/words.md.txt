See See the release notes. <!-- want "repetition.adjacent-word" -->

The scanner requires requires an external decoder. <!-- want "repetition.adjacent-word" -->

The client had had enough time to close the connection.

We know that that request failed.

The example says "See See the release notes".

The scanner `requires requires` an external decoder.
