## Drift

The command exits `1` because drift was found, which confirms the check worked. <!-- want "repetition.repeated-claim" -->

```text
changed
```

The command exits `1` because drift was found.

The command exits `0` because drift was found.

## Another command

The command exits `1` because drift was found.
