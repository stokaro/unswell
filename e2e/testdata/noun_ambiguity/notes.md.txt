Package document defines source coordinates and the neutral prose model.

Emit latches validation failures even when a custom rule ignores the error.

The access control policy requirements apply. // want "syntax.noun-stack"

A binary origin probability cannot measure the fraction of words written by AI.

The service request response status code cannot change. // want "syntax.noun-stack"

The analysis completion state applies to every configured block in the document.

Reader validation cannot reconstruct feature input hashes without the original inputs.

The prototype vet driver check comments remain disabled in every builtin profile.
