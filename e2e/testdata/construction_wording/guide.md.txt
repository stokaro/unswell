The café queue accepts two writers, and the **difference** is worth knowing because stale clients must retry. <!-- want "filler.evaluative-closure" -->

It should be noted that the client retries only after a timeout. <!-- want "filler.evaluative-closure" -->

This is done by defining `retry_limit`. <!-- want "filler.instruction-scaffolding" -->

If you need a horizontal divider you just need to put three dashes on a line. <!-- want "filler.instruction-scaffolding" -->

Neither definition is repeated here. <!-- want "filler.document-justification" -->

The subset is still a subset, and the reference lists its members. <!-- want "repetition.definition-echo" -->

The category label describes the category. <!-- want "repetition.redundant-predicate" -->

It must be noted in the audit log before the process exits.

This is done by the next worker.

The subset is still a proper subset.

The `category` label describes the category.

It is worth measuring latency because the cache changes it.
