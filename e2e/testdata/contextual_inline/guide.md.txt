# Inline context

Café 🙂. The `request` is rejected rather than queued. The `response` is saved instead of discarded. <!-- want "syntax.paired-contrast-density" -->

## Different contrasts

The command reads `source`, not `target`. The check reads `state`, not `history`. <!-- want "syntax.paired-contrast-density" -->

## Protected keywords

The request uses `rather than` in its example. The response uses `instead of` in its example.
The request is rejected rather `example` than queued. The response is saved instead `example` of discarded.

## Additive wording

The command reads files, not only directories. The check reads rows, not just tables.

## Separate windows

The request is rejected rather than queued.

```text
The response is saved instead of discarded.
```

The response is saved instead of discarded.

## Necessary singleton

The digest covers the plan, not the clock.

## Revised wording

The command reads `source`; it does not read `target`. The check reads `state` and excludes `history`.
