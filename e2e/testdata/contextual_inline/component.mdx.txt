import { Aside } from '@astrojs/starlight/components';

<Aside>
The `request` is rejected rather than queued. The `response` is saved instead of discarded. <!-- want "syntax.paired-contrast-density" -->
</Aside>

## Expressions

{"The request is rejected rather than queued. The response is saved instead of discarded."}
