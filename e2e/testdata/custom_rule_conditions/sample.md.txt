The **very** small client uses a very short timeout. <!-- want "company.repeated-very" -->

A very small client uses a short timeout.

A `very very` small client uses a short timeout.

Quoted wording: a very small client uses a very short timeout.
