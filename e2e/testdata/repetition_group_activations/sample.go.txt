package sample

// The request includes every header required by the external service and checks the returned status. // want "repetition.exact-sentence"
func Check() {}

const message = "The request includes every header required by the external service and checks the returned status."
