# Repetition groups

Each connection has a timeout that the client checks before sending another request to the server.

The service accepts a request from the client and sends the response with its credentials. The service accepts a request from the client and sends the response with its credentials. // want "repetition.exact-sentence"

The **client** opens a connection to the database. The client opens a file from the disk. The client opens a channel to the service. // want "repetition.sentence-openers"

A writer reviews the document before the team publishes the changes. // want "repetition.sentence-openers" "repetition.paragraph-openers"

A writer reviews the report before the manager sends the results.

A writer reviews the proposal before the group makes its decision.

Hi. A new sentence contains enough words to meet the minimum while leaving the first sentence short.

The `caller` eventually waits until every pending request receives a response from the configured external service.

The client waits.
