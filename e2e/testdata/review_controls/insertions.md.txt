Issues (#20), (#56), and (#57) track this work, and the libraries (MIT) keep their notices.

The client (which must not retry when RetryLimit is 0 even if the server has closed the first connection) reports the error to the caller. // want "syntax.parenthetical-load"
