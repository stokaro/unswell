/**
 * @example
 * const result = formatISO9075(new Date(2019, 8, 18, 19, 0, 52), { representation: 'time' })
 */
function formatExample() {}

// It is important to note that the timeout is 10 seconds unless RetryLimit is 0. // want "filler.announced-importance"
function timeout() {}
