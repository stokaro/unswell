The analysis completion state applies to the entire collection.

It cannot reconstruct feature input hashes without the original inputs.

The probability pack column digest does not cover its declared columns. // want "syntax.noun-stack"
