The client opens the connection &mdash; then checks the reply from the server. The client opens the connection &mdash; then checks the reply from the server. The client opens the connection &mdash; then checks the reply from the server. The client opens the connection &mdash; then checks the reply from the server.  // want "format.em-dash-density"

The connection closes.
