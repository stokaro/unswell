# Development decisions before confirmation

The exposed baseline fully detects 146 of 947 frozen defects across 160 pages.
This is the merged quotation repair at `f32dcd15f2ab9fac18fade9c840c0bf865e54277`,
remeasured in both profiles. The previous discourse candidate was rejected, so
its **before** judgments are the inheritance source. The newly detected unquoted
`when when` duplicate receives its own reviewed disposition.

The first candidate added 47 warnings in each profile. The same implementation
assistant reviewed every addition against its source and frozen editing operation
in `development-screening.json`. An overlapping range alone receives no credit.
Additional useful edits stay separate from frozen recall. The status names in
that screening record are review categories, not production severities.

The subject-cleft hypothesis is rejected before confirmation. Of its 26 warnings,
16 have unresolved or defensible focus, one does not diagnose the labeled edit,
and nine support a wording repair (two full frozen events, one partial event and
six additional repairs). A whole-sentence contrast guard removes three disputed
contrasts, but also a full frozen event and an additional repair. The retained
`development-candidate-v2.tar.gz` records this unsuccessful guard revision. Its
archive hash and the affected review rows are in the screening record. No
subject-cleft rule is included in the confirmation candidate.

The other hypotheses continue:

- Nominal use, manner and frequency support: fourteen exposed additions, one full
  frozen event, three partial events and ten additional edits. These findings do
  not qualify unrelated assertions inside the same source ranges.
- Repeated relationship markers: six additions, two full frozen events, one
  partial event, two additional edits and one uncertain scope. The uncertain
  containerd sentence has a finite predicate and a separate infinitive action.
  The candidate now excludes that structure as well as separate finite
  predicates. A prepositional `to` followed by a noun remains eligible; public
  API tests cover both cases.
- An immediately adjacent nominal instrument extends one existing instruction
  warning to include its method, covering both parts of the frozen edit.

The source reviews and all 63 confirmation defects remain unchanged. Confirmation
will compare the complete frozen candidate with merged main on all twelve pages,
including the two Ptah pages labeled without defects. It will retain all findings,
controls, uncertainty, partial repairs and missed events. No runtime changes will
be made in response to those outputs within this experiment.

These development gains do not establish useful whole-page recall. The much
larger missed-event inventory remains the target; a successful narrow construction
cannot stand in for the 80% recall and 85% soft-diagnostic precision goals.
