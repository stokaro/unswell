# Remaining new-page defects

These 32 frozen events are not fully diagnosed. One has partial coverage. No count or origin label substitutes for the stated wording repair.

## c02-d01: wordiness

The selector-says-what-it-takes wrapper obscures a direct introduction to the input choices.

Select the desired schema source with one of these options; retain the table of accepted inputs.

> One selector names the desired schema, and each says what it takes:

## c03-d01: empty_framing

Reader presumption and page announcement delay the concrete verification gates.

Introduce the three gates directly, retaining the distinction from SQL linting.

> You are about to let migrations touch a database other people depend on. This page shows the gates that decide whether the directory in front of you is the one you reviewed:

## c03-d02: empty_framing

The defensive noticed-versus-class contrast adds no membership condition.

State that the gate covers commands that execute migration SQL.

> not a list of verbs that happened to be noticed

## c03-d03: empty_framing

Intentionality endorsement adds no behavior to the execution-versus-metadata distinction.

Delete this clause and retain what each recovery command changes.

> and the distinction is deliberate.

## c03-d04: empty_framing

The author-defense contrast repeats the stated checksum-writing exception.

Keep why these commands can rewrite an out-of-date checksum without defending the implementation choice.

> they are outside the class by the predicate, not by oversight.

## c03-d05: vague_claims

The superlative ranks omissions without a comparison; the next text supplies the actual destructive risk.

Keep the rollback risk without ranking historical omissions.

> it was the worst one to leave out.

## c03-d06: empty_framing

The needs-it assertion repeats purpose without identifying an additional use condition.

Start with the concrete rollback use case and retain override and no-rehash behavior.

> It exists for the case that genuinely needs it

## c03-d07: empty_framing

The reason announcement precedes the actual tampered-directory append mechanism.

Start with that mechanism and retain the covered writing commands.

> The verbs that **write** are on that list for a reason of their own.

## c03-d08: empty_framing

Abstract contrast delays the actual intentional-versus-accidental recovery choices.

Present the two recovery cases directly.

> Recovery is a decision, not a command:

## c03-d09: wordiness

Question/answer and intentionality framing obscure a direct comparison of missing versus mismatched checksums.

Name missing and mismatched checksum states, then compare native and compatible command behavior.

> Drift is one question; "there is no sum file at all" is a different one, and the two surfaces answer it differently on purpose.

## c03-d10: empty_framing

The recursive worth slogan adds no trust condition before the concrete artifact-provenance explanation.

Start with the trusted-source requirement and retain the artifact and write-permission constraints.

> A sum check is worth what the sum's provenance is worth.

## c03-d12: empty_framing

The implementation-defense contrast adds no operation beyond the shared walker described next.

Keep the walker mechanism and the files it includes.

> and not by special-casing

## c03-d13: empty_framing

The ordinal attention marker and cost metaphor delay the concrete version-skipping consequence.

Start with how the B10 baseline skips lower-version objects.

> The first row is the one that costs you something.

## c03-d14: empty_framing

Purpose endorsement repeats the immediately described fresh-install behavior.

Keep the no-history execution behavior without this sentence.

> That is the fresh-install path a converted Flyway directory exists for.

## c03-d15: empty_framing

The way-forward/dead-end contrast adds no behavior to the workaround.

Delete this conclusion and retain what the flag permits and its limits.

> which is what makes the flag a way forward rather than a second dead end.

## c03-d16: vague_claims

An unscoped importance ranking adds no requirement to downward preconditions.

Keep the separate downward precondition behavior without ranking its value.

> which is where a precondition is often worth asserting most.

## c04-d01: empty_framing

Self-deprecating author commentary supplies no type-size behavior or maintenance instruction.

Delete the comment; retain the three type-size checks.

> # we're just getting lazy now.

## c04-d02: wordiness

Repeated we-do-not-use-it questions and the capitalized complaint obscure the reason for disabling the check.

State once that the unused file-offset check is disabled, retaining attribution and the config-header concern if actionable.

> # XXX: Why is this here? we don't even use it. Well, we don't even use it # on top of that, why is it set in the config.h?! IT_MAKES_NO_SENSE # I'm commenting it out for now. # - ellzey

## c05-d01: empty_framing

Generic know/start/right-path framing adds no prerequisite before the actual contribution instructions.

Remove the sentence and begin with the contribution workflow.

> Here are a few things to know to get you started on the right path.

## c05-d02: wordiness

The process-existence wrapper delays the responsible reviewer and exception list.

Name knsv (Knut Sveidqvist) as the primary reviewer, then list the exceptions.

> As we have many pull requests from developers new to mermaid, we have put in place a process, wherein *knsv, Knut Sveidqvist* is the primary reviewer of changes and merging pull requests. It is as follows:

## c05-d03: empty_framing

The sad/effort rhetorical questions substitute emotional pressure for the documentation requirement.

Require documenting the feature and link the editing instructions.

> If it is not in the documentation, it's like it never happened. Wouldn't that be sad? With all the effort that was put into the feature?

## c05-d04: wordiness

Relative strictness commentary adds no permission beyond the following collaborator-only rule.

Start with collaborators may commit documentation directly to develop.

> We are a little less strict here,

## c05-d05: needless_repetition

The same documentation-location guidance appears twice without a new constraint.

Keep one location reference; retain any distinct navigation instructions.

> The docs are located in the `docs` folder and are written in MarkDown.

> The documentation is located in the `docs` directory and organized according to relevant subfolder.

## c05-d06: wordiness

Repeated important/important and a long nested contributor description obscure the regression-test purpose; impossible overstates the refactoring constraint.

Explain that parser tests expose grammar regressions to other contributors and protect refactoring.

> This is important so that, if someone else does a change to the grammar that does not know about this great feature, gets notified early on when that change breaks the parser. Another important aspect is that without proper parsing tests refactoring is pretty much impossible.

## c05-d07: empty_framing

Generic success reassurance repeats the preceding visual-review mechanism.

Keep the rendering-review mechanism without the final exclamation.

> Less chance that it breaks!

## c05-d09: vague_claims

The universal no-one claim overstates the effect of absent documentation.

Require documenting the feature without asserting universal discovery and use outcomes.

> if it is not in the documentation, no one will know about it and then **no one will use it**.

## c05-d10: empty_framing

Repeated emotional questions add no documentation action.

Delete the questions and retain the documentation instructions.

> Wouldn't that be sad? With all the effort that was put into the feature?

## c05-d11: vague_claims

Blanket praise and an absolute only-encouraging promise are unsupported.

Keep the concrete invitation to ask for help in the Slack forum.

> We have a great community with only encouraging words.

## c06-d01: vague_claims

Unscoped useful/good judgments add no setup behavior before the configuration instructions.

Begin with the example configuration and retain self-scraping as the exercise.

> While a Prometheus server that collects only data about itself is not very useful, it is a good starting example.

## c06-d02: empty_framing

Announces exploration before the exact expression-browser instructions without adding a prerequisite.

Start with the expression-browser location and query.

> Let us explore data that Prometheus has collected about itself.

## c06-d03: wordiness

The reader-can-gather wrapper delays the metric description.

Describe the metric directly and retain the metrics endpoint as a reference.

> As you can gather from [localhost:9090/metrics](http://localhost:9090/metrics),

## c06-d04: empty_framing

Announces the configuration action already identified by the section before the actual grouping instructions.

Begin with grouping the three endpoints into the node job, retaining production/canary labels.

> Now we will configure Prometheus to scrape these new targets.
