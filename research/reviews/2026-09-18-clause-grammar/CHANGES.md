# Reviewed diagnostic changes

Indices refer to the retained strict reports. Findings and dispositions for both profiles are saved.

## exposed_repetition: added 36 (filler.instruction-scaffolding)

Provides-the-ability adds a support layer before the concrete capability. The comparison with finite-automaton worst-case linear time must stay. This edit was found after diagnostics and is outside the older narrow repetition review.

sources/c06.md bytes 14759–14904:

> The former provides the ability to add lots of bells and whistles over the latter, but the latter executes with worst case linear time complexity

## exposed_boundaries: added 1 (filler.evaluative-closure)

The reader-attention selection matches the frozen endorsement; keep explicit undecided reporting and graph incompleteness.

sources/c02.mdx bytes 1661–1716:

> which is the part of the output a reader has to look at

## exposed_boundaries: added 8 (filler.evaluative-closure)

The whole-point-of-reading statement is the frozen purpose endorsement, separate from the following none-versus-unknown limitation.

sources/c02.mdx bytes 11209–11264:

> That distinction is the whole point of reading the list

## exposed_boundaries: added 26 (filler.instruction-scaffolding)

Supports-the-ability names the frozen redundant capability layer. Preserve regular-expression patterns and the link destination.

sources/c04.md bytes 3128–3205:

> But ripgrep supports the ability to specify patterns via [regular expressions

## exposed_boundaries: added 30 (filler.unscoped-assurance)

Restores the frozen way-the-flag-functions simplicity judgment; preserve the actual glob definition.

sources/c04.md bytes 13518–13563:

> The way the `--type` flag functions is simple

## exposed_boundaries: added 32 (filler.unscoped-assurance)

Restores the frozen setup-is-simple judgment while keeping environment-variable selection and file-format requirements.

sources/c04.md bytes 20046–20087:

> Setting up a configuration file is simple
