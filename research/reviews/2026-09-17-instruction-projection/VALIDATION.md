# Validation record

The measured runtime is `d2989e83389751b0fbf2f9d55e0cff8546002fe2`.
Subsequent edits contain evidence, documentation and expected rule identity hashes;
the runtime remains unchanged after the confirmation output was opened.

- Public-API and compiled CLI regressions passed for instruction projection,
  adjacent method ranges, support-chain forms, controls, Unicode, source contexts,
  occurrence policy, exemptions and cancellation.
- Strict lint passed in all six checked Go modules.
- The first full check passed runtime tests and stopped on ten feature golden
  files containing the previous rule identity. Exactly 26 `ruleset_hash` fields
  were updated; feature values and diagnostics in those fixtures did not change.
- The evidence validator and its 13 tests pass. Negative probes reject missing
  finding reviews, cross-page and incidental-length credit, source drift, reused
  confirmation sources and additional post-diagnostic claims promoted to recall.
  Partial coverage stays partial; the Gantt regression verifies that both related
  ranges are required for the new complete event.

All required stages of `make check` passed. The sandbox could not expose resource
reporting through `/usr/bin/time`; `check-performance`, `check-research-cost` and
`check-reproducible` passed outside the sandbox. Policy, tidy, all ordinary module
tests, strict lint, 35 Bash scripts, mirror/SBOM rejection probes and report schema
checks passed. Six Linux/macOS/Windows amd64/arm64 binaries reproduced byte for
byte on this host with cgo disabled.

CLI/MCP self-checking matched evidence for 1026 documents; the failure, rewrite
and malformed-input probes passed. The working-tree scan reported 820 findings
and passed its gate. Review of the new evidence documentation retained technical
readability measurements, surface passive/contrast notes and immutable protocol
wording. One long sentence in the generated report was split after that scan.
A final rebuild and CLI/MCP check follow the evidence commit; no runtime semantics
changed after the measured commit.
Race detection, active fuzzing and coverage remain deferred under #123. No Docker
resources were created. This record does not establish terminal remote CI,
merging, deployment, model qualification or satisfactory broad editorial recall.
