# Remaining confirmation defects

All 54 lack full coverage in both profiles. No partial event receives full credit.

## c01-d01: empty_framing

The broad opening announces an idea before giving the independently usable desired-schema definition.

Proposed edit: Start with describing the desired schema; retain comparison, migration and workflow meanings.

Bytes 485–537

```text
Everything in Ptah starts from one declarative idea:
```

## c02-d01: vague_claims

The frequency ranking has no stated population or measurement; the special required-without-a-cap condition is independently useful.

Proposed edit: State that truncate is required even when max_input_bytes has no cap; omit or substantiate the frequency ranking.

Bytes 1581–1619

```text
truncate` is the one most often missed
```

## c02-d02: needless_repetition

This personified tail repeats the immediately preceding explanation that catch-up starts after the backfilled snapshot.

Proposed edit: Keep the ordering requirement and snapshot-range explanation; remove the repeated tail.

Bytes 3940–3998

```text
the changes it would read are ones
the backfill still owes
```

## c02-d03: empty_framing

This blanket cost announcement precedes the exact provider-request, vector-write and watermark facts.

Proposed edit: Keep the three exact effects; omit the broad cost announcement.

Bytes 4001–4040

```text
Nothing is spent to reach this refusal.
```

## c02-d04: empty_framing

This evaluates normality before the concrete reason that the source continues changing.

Proposed edit: Keep the source-change reason and phase link without judging the behavior ordinary.

Bytes 4282–4299

```text
That is ordinary,
```

## c02-d05: unjustified_intensifiers

Different schemas already defines the identity distinction; genuinely adds emphasis rather than a test.

Proposed edit: Use different schemas and retain identical table-name handling.

Bytes 6454–6473

```text
genuinely different
```

## c02-d06: empty_framing

The ordinary-state judgment repeats the preceding two-generations/one-source example without an additional contract.

Proposed edit: Retain content-addressed generation identity and the two-spelling result.

Bytes 6747–6801

```text
which is the
ordinary state during a generation change
```

## c02-d07: vague_claims

The frequency qualifier is not tied to measured verification outcomes.

Proposed edit: Name the circumstances under which freshness and coverage indicate unfinished work, or omit the frequency claim.

Bytes 8365–8378

```text
almost always
```

## c02-d08: vague_claims

The sensible-default endorsement does not explain why exact approval is required.

Proposed edit: Keep the enabled policy and exact-digest fix; explain approval binding if a rationale is needed.

Bytes 8551–8580

```text
which is the sensible default
```

## c02-d09: empty_framing

This evaluates the mechanism before giving the exact protection it provides.

Proposed edit: Say the refusal prevents approval of a different plan; retain the instruction to read and approve the new plan.

Bytes 8963–9000

```text
The refusal is the mechanism
working:
```

## c03-d01: empty_framing

This repeats the inventory/default/reason announcement in the opening under an At a glance heading.

Proposed edit: Use the table directly; retain each default, reason and measured CLI difference in the entries.

Bytes 1801–1915

```text
Each entry below carries the default, the reason for it, and what the pinned
community CLI does on the same input.
```

## c03-d02: wordiness

This restates the immediately established missing-module failure as a slogan.

Proposed edit: Retain the no-opt-in condition and exact missing-module error, then the planned-versus-existing distinction.

Bytes 8180–8227

```text
and no value of a variable makes a module exist
```

## c03-d03: wordiness

The layered plan/report/operator explanation defends the default indirectly instead of explaining report-only behavior.

Proposed edit: Keep the report-only default and the pipeline opt-in that lets an error finding decide the exit code.

Bytes 12011–12149

```text
A plan is a document an operator
reviews and approves, and a report that refuses on their behalf is one they
cannot approve anything with.
```

## c03-d04: wordiness

The rules-versus-plan metaphor indirectly restates the limited hazard coverage.

Proposed edit: State directly that no findings does not guarantee a safe plan; retain the stderr disclosure.

Bytes 12427–12496

```text
so a report
without findings describes the rules rather than the plan
```

## c03-d05: wordiness

Measured-versus-chosen framing and the repeated refuses wording obscure the direct compatibility rule.

Proposed edit: Say that the refused kinds match the pinned community CLI results; keep the exact kind lists.

Bytes 13890–14011

```text
Which kinds refuse is measured rather than chosen: a repeat is refused where the
community CLI refuses the same document.
```

## c03-d06: empty_framing

The floor metaphor repeats the already described accepted-input difference instead of naming that compatibility condition.

Proposed edit: Say refusal is opt-in because the community CLI accepts those four kinds.

Bytes 14779–14855

```text
Refusing them is above the drop-in
floor rather than on it, so it is opt-in.
```

## c03-d07: empty_framing

The broad ranking endorses the preceding concrete mismatch without specifying another consequence.

Proposed edit: Keep that removing the default misrepresents the database; omit the comparative endorsement.

Bytes 16842–16887

```text
which is the one outcome worse than a refusal
```

## c03-d08: empty_framing

The repeated promise wording advertises guarantees already stated as explicit output and reporting behavior.

Proposed edit: State the compatibility limitation, self-consistent output and reporting/opt-in behavior directly, without promise framing.

Bytes 16890–17177

```text
So `ptah-compat schema inspect` is not a promise that every PostgreSQL database
produces community-CLI-readable HCL. It is a promise that the output is always
self-consistent, that nothing disappears without being reported, and that the
full description is one environment variable away.
```

## c04-d01: wordiness

The primary-means/via/classes framing delays the direct relationship between a model and BaseModel.

Proposed edit: Say objects are defined as models, which are classes inheriting from BaseModel; retain the definition without simply.

Bytes 0–126

```text
The primary means of defining objects in *pydantic* is via models
(models are simply classes which inherit from `BaseModel`).
```

## c04-d02: empty_framing

The iceberg metaphor advertises breadth immediately before a concrete method inventory.

Proposed edit: Omit the metaphor and keep the methods and attributes inventory.

Bytes 2389–2463

```text
The example above only shows the tip of the iceberg of what models can do.
```

## c04-d03: wordiness

Predicted desire and convenience judgments wrap the actual reserved-field alias instruction.

Proposed edit: To name a column after a reserved SQLAlchemy field, use a field alias; retain the population-priority warning.

Bytes 4826–4938

```text
You may want to name a Column after a reserved SQLAlchemy field. In that case, Field aliases will be
convenient:
```

## c04-d04: unjustified_intensifiers

`Very` adds emphasis while the next clause names the exact dictionary-versus-keyword-argument difference.

Proposed edit: Use similar and retain the argument and ValidationError distinction.

Bytes 8364–8376

```text
very similar
```

## c04-d05: needless_repetition

The same example reference is announced twice before the conditional result.

Proposed edit: Use In the example above and keep the omitted-fields-set condition and exact field set.

Bytes 10439–10473

```text
For example, in the example above,
```

## c04-d06: vague_claims

Easier asserts an unmeasured user benefit; reuse is the concrete purpose.

Proposed edit: State support for reusing a common model structure through generic models without the ease claim.

Bytes 10639–10690

```text
to make it easier to reuse a common model structure
```

## c04-d07: wordiness

The perform-the-following-steps layer is unnecessary before an explicit ordered procedure.

Proposed edit: Introduce the steps with To declare a generic model; preserve their order and all type parameters.

Bytes 10879–10948

```text
In order to declare a generic model, you perform the following steps:
```

## c04-d08: wordiness

Predicted future desire obscures which fields receive replaceable type parameters.

Proposed edit: Use TypeVar annotations for fields whose types will be replaced; retain support for other pydantic models.

Bytes 11256–11329

```text
where you will want to replace them with other types or
  pydantic models
```

## c04-d09: unjustified_intensifiers

The example demonstrates reuse but does not define a measure or prerequisite for ease.

Proposed edit: Use reusable and retain the HTTP response payload wrapper example.

Bytes 11385–11398

```text
easily-reused
```

## c04-d10: wordiness

Expected type checking and the hypothetical non-generic declaration repeat the integration claim indirectly.

Proposed edit: Say mypy type-checks concrete generic models as it does explicitly declared types; keep the concrete comparison.

Bytes 11768–11950

```text
Pydantic's generics also integrate properly with mypy, so you get all the type checking
you would expect mypy to provide if you were to declare the type without using `GenericModel`.
```

## c04-d11: wordiness

The when-it-comes-to layer delays the two generic-parameter cases.

Proposed edit: Introduce the unparameterized and bounded-TypeVar cases directly, preserving their separate behavior.

Bytes 13173–13236

```text
when it
comes to leaving them unparameterized, or using bounded
```

## c04-d12: empty_framing

This merely announces the adjacent example after the branch list has already introduced the behavior.

Proposed edit: Keep the branch list and example; remove the generic demonstration announcement.

Bytes 15703–15749

```text
This is demonstrated in the following example:
```

## c04-d13: wordiness

The reader-desire condition and can-implement wrapper delay a concrete iteration and indexing instruction.

Proposed edit: To access or iterate over root items directly, implement the named methods; retain both methods and the example.

Bytes 16066–16256

```text
If you want to access items in the `__root__` field directly or to iterate over the items, you can implement custom `__iter__` and `__getitem__` functions, as shown in the following example.
```

## c04-d14: vague_claims

The personal judgment supplies no technical condition for mutability.

Proposed edit: Describe the operations that can mutate the object; retain the shallow-immutability caveat without judging developers.

Bytes 16624–16659

```text
If developers are determined/stupid
```

## c04-d16: wordiness

Two may-want announcements obscure a per-instance-default instruction.

Proposed edit: For a default computed for each model instance, use default_factory; preserve the beta warning below.

Bytes 19674–19834

```text
When declaring a field with a default value, you may want it to be dynamic (i.e. different for each model).
To do this, you may want to use a `default_factory`.
```

## c04-d17: unjustified_intensifiers

Extremely adds an unmeasured degree to a feedback request.

Proposed edit: Ask for feedback through the two listed channels without the intensity claim; retain beta and version constraints.

Bytes 20165–20181

```text
extremely useful
```

## c04-d19: wordiness

The utility/can-be-used/apply-logic sequence obscures the direct parsing API distinction.

Proposed edit: Say parse_obj_as parses values into arbitrary pydantic-compatible types, unlike model-bound parse_obj; keep both method identities.

Bytes 21478–21749

```text
Pydantic includes a standalone utility function `parse_obj_as` that can be used to apply the parsing
logic used to populate pydantic models in a more ad-hoc way. This function behaves similarly to
`BaseModel.parse_obj`, but works with arbitrary pydantic-compatible types.
```

## c04-d20: wordiness

Capable of parsing can be expressed as can parse without changing possibility or supported types.

Proposed edit: Use can parse and retain the exact supported-field-type range.

Bytes 21991–22012

```text
is capable of parsing
```

## c04-d21: vague_claims

The broad usefulness ranking is unsupported by the local conversion example.

Proposed edit: Keep the intentional conversion policy and discussion link; omit or substantiate the usefulness ranking.

Bytes 22587–22627

```text
in general it's the most useful approach
```

## c05-d01: vague_claims

The status claim has no named standard, adoption measure or scope.

Proposed edit: Name the concrete runtime role and supported platforms without the unspecified standard claim.

Bytes 1333–1350

```text
industry-standard
```

## c05-d02: vague_claims

The three broad quality claims lack definitions or measurements, while the next sentence lists concrete functions.

Proposed edit: Use the concrete lifecycle and platform description; define a property before claiming it.

Bytes 1369–1427

```text
with an emphasis on simplicity, robustness and portability
```

## c05-d03: wordiness

The reader-interest condition wraps a direct link to an example.

Proposed edit: Link to the Getting Started example for trying containerd without predicting reader interest.

Bytes 2093–2202

```text
If you are interested in trying out containerd see our example at [Getting Started](docs/getting-started.md).
```

## c05-d04: vague_claims

`Very minimal` gives neither a resource measure nor a prerequisite list; the paragraph already explains delegation and the runc version reference.

Proposed edit: Start with delegated runtime support and the actual dependency/version reference.

Bytes 2589–2642

```text
Runtime requirements for containerd are very minimal.
```

## c05-d05: wordiness

Understood caveat is indirect and assumes prior knowledge of the qualification.

Proposed edit: State the distribution-kernel-version caveat directly and keep the recommended kernel baseline.

Bytes 3081–3136

```text
With the understood caveat of distro kernel versioning,
```

## c05-d06: unjustified_intensifiers

Full gives no completeness criterion for the client package.

Proposed edit: Use client package and keep the integration purpose.

Bytes 3815–3834

```text
full client package
```

## c05-d07: wordiness

The narrator/help layer can state what the built-in functions generate directly.

Proposed edit: Say built-in functions generate runtime specifications from images and custom parameters; retain both inputs.

Bytes 5281–5397

```text
We have built in functions to help you generate runtime specifications based on images as well as custom parameters.
```

## c05-d09: wordiness

The predicted discovery adds a layer before the distribution-packaging fact.

Proposed edit: State directly that distributions package containerd across architectures; retain the Ubuntu example.

Bytes 9294–9312

```text
you will find that
```

## c05-d10: empty_framing

The unsupported best-place endorsement repeats the preceding instruction to use issues and pull requests for long discussions.

Proposed edit: Keep the asynchronous communication instruction without ranking its quality.

Bytes 10423–10488

```text
This will be the best place to discuss design and implementation.
```

## c05-d11: wordiness

The repeated common/all/projects/repository wording obscures the single shared-governance location.

Proposed edit: State that containerd projects share governance, maintainership and contributing guidelines in containerd/project; retain the primary-project distinction.

Bytes 11447–11730

```text
containerd** is the primary open source project within the broader containerd GitHub repository.
However, all projects within the repo have common maintainership, governance, and contributing
guidelines which are stored in a `project` repository commonly for all containerd projects.
```

## c05-d12: wordiness

The please-find-all-these setup adds a layer before the linked project documents.

Proposed edit: Introduce the governance, maintainer and contribution links directly, retaining their repository location.

Bytes 11732–11792

```text
Please find all these core project documents, including the:
```

## c05-d13: empty_framing

The rhetorical invitation precedes the adopter-list action without adding an eligibility condition.

Proposed edit: Keep the adopter list and the instruction for projects using containerd.

Bytes 12172–12214

```text
Interested to see who is using containerd?
```

## c06-d01: empty_framing

The promised inspiration is an unmeasured benefit beyond the concrete avoided-duplicate-work reason.

Proposed edit: Keep the discussion-first instruction and avoided-work rationale without promising inspiration.

Bytes 591–646

```text
and surely give you and us a good deal
  of inspiration
```

## c06-d02: wordiness

The attention wrapper can become the direct sign-off requirement.

Proposed edit: Use Sign off and retain the DCO link and requiredness.

Bytes 1111–1130

```text
Be sure to sign off
```

## c06-d03: wordiness

Wish-to and want-to repeat the contributor intention around the concrete issue-claim action.

Proposed edit: Before working on an issue, claim it with a comment; retain the duplication-avoidance reason.

Bytes 1214–1335

```text
Should you wish to work on an issue, please claim it first by commenting on the GitHub issue that you want to work on it.
```

## c06-d04: wordiness

The preferred-course-of-action judgment states the fix-first policy indirectly.

Proposed edit: Prefer fixing the code; suppress a warning only when justified, preserving the false-positive exception.

Bytes 2584–2715

```text
Use this sparingly though, fixing the code to comply with the linter's recommendation is in general the preferred course of action.
```

## c06-d05: unjustified_intensifiers

Easiest ranks the chat client without a stated user context or criterion.

Proposed edit: Retain the Riot link as an option without ranking ease.

Bytes 3643–3664

```text
for the easiest start
```
