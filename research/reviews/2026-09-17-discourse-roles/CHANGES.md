# Reviewed changes

Indices refer to retained strict reports. Exact before and after judgments remain in dispositions.json.

## development / added

### 95: filler.evaluative-closure

The first-person attention notice delays the library-release warning, exactly as frozen. Preserve the first-release and unstable-API facts.

Status: actionable. Full: p015-d02. Partial: none.

`sources/p015.md` bytes 7244–7311

```text
I want to stress that this is the very first release of the library
```

### 98: filler.unscoped-assurance

The single ease assertion has no audience or concrete step. The frozen defect is the repetition of two assurances, and this diagnostic identifies only one; do not credit that repetition event.

Status: additional_actionable. Full: none. Partial: none.

Additional repair: Remove the fairly-straightforward sentence and keep the two branch-specific backport paths.

`sources/p016.md` bytes 6632–6679

```text
Opening a backport PR is
fairly straightforward
```

## exposed_framing / added

### 0: filler.evaluative-closure

The reader-error announcement adds no maintenance condition; the following paragraph explains it.

Status: actionable. Full: c02-d01. Partial: none.

`sources/c02.md` bytes 1195–1223

```text
This is the part people miss
```

### 10: filler.evaluative-closure

The gerund purpose cleft repeats the compatibility rationale; retain unchanged output and the loss report.

Status: actionable. Full: c03-d01. Partial: none.

`sources/c03.md` bytes 6722–6778

```text
matching the community binary is what the surface
is for
```

## exposed_local / added

### 73: filler.unscoped-assurance

The surrounding paragraph states a coarser clock-resolution tradeoff and provides a precise-timer opt-out. The warning isolates its invocation-cost comparison from that technical context; absence of a numeric benchmark alone does not establish an editorial defect here.

Status: nonactionable. Full: none. Partial: none.

`sources/c11.txt` bytes 25151–25202

```text
and is much faster
  to invoke than CLOCK_MONOTONIC
```

## exposed_context / added

### 49: filler.evaluative-closure

The importance announcement repeats the already explained issue-template consequence and can be removed.

Status: actionable. Full: c10-d02. Partial: none.

`sources/c10.md` bytes 3299–3326

```text
This step is very important
```

## exposed_instruction / removed

### 168: filler.instruction-scaffolding

The can-be-used-to construction could be shorter, but the frozen review left this aggregation introduction uncertain because the input, output and dimensionality matter. Keep it unresolved.

Status: uncertain. Full: none. Partial: none.

`sources/c10.md` bytes 7971–8173

```text
Prometheus supports the following built-in aggregation operators that can be
used to aggregate the elements of a single instant vector, resulting in a new
vector of fewer elements with aggregated values
```

## exposed_construction / added

### 38: filler.evaluative-closure

The gerund purpose restatement adds no bypass condition or repair step; keep the refusal and unique-index consequence.

Status: actionable. Full: c03-d10. Partial: none.

`sources/c03.md` bytes 36371–36417

```text
retrying the body is what the refusal is
about
```

## exposed_purpose / removed

### 18: filler.instruction-scaffolding

The relative support chain is diagnosed, but the repeated informational-label/additional-information wording is not specifically identified. Award partial coverage of the compound defect only.

Status: actionable. Full: none. Partial: c06-d02.

`sources/c06.md` bytes 1393–1557

```text
The `annotations` clause specifies a set of informational labels that can be used to store longer additional information such as alert descriptions or runbook links
```

## exposed_verb / added

### 19: filler.evaluative-closure

The diagnostic isolates the importance predicate. Preserve the distinct factual continuation that both tools agree and recover under the default transaction mode.

Status: actionable. Full: c03-d08. Partial: none.

`sources/c03.md` bytes 20408–20450

```text
The third row is the one that matters most
```

## exposed_relations / added

### 0: filler.evaluative-closure

The abstract purpose announcement repeats the signature role before a concrete tampering exercise.

Status: actionable. Full: c02-d01. Partial: none.

`sources/c02.md` bytes 6697–6730

```text
This is what the signature is for
```

### 20: filler.unscoped-assurance

The ease claim adds no POST instruction. Preserve the data option and URL-encoding requirement.

Status: actionable. Full: c04-d06. Partial: none.

`sources/c04.md` bytes 9049–9082

```text
It's easy to post data using curl
```

## exposed_proposition / removed

### 12: filler.instruction-scaffolding

The checksum is an operand used for caching, not an actor that itself caches. The relative clause explains its role in the dependency-derived cache key; an action-support diagnosis does not establish a safe direct action here.

Status: nonactionable. Full: none. Partial: none.

`sources/c04.md` bytes 6338–6489

```text
It contains a digest that is combined with the cache keys of the inputs to determine the stable checksum that can be used to cache the operation result
```

## exposed_clauses / removed

### 34: filler.instruction-scaffolding

The relative can-be-used-to/apply-parsing-logic chain identifies the frozen indirect API description. Keep both method identities and the subsequent distinction about arbitrary compatible types.

Status: actionable. Full: c04-d19. Partial: none.

`sources/c04.md` bytes 21478–21638

```text
Pydantic includes a standalone utility function `parse_obj_as` that can be used to apply the parsing
logic used to populate pydantic models in a more ad-hoc way
```

## exposed_action / added

### 10: filler.evaluative-closure

The column-worth-reading selection is the frozen attention judgment. Keep the durable-tag and cross-client distinction.

Status: actionable. Full: c03-d06. Partial: none.

`sources/c03.md` bytes 15540–15583

```text
the source column is the part worth
reading
```

### 27: filler.unscoped-assurance

The expressly speculative speed claim has no workload or result; keep the epollfd versus socketpair change.

Status: actionable. Full: c04-d17. Partial: none.

`sources/c04.txt` bytes 13410–13441

```text
This is supposed to be
  faster
```

### 28: filler.unscoped-assurance

The speculative speed magnitude lacks scope. Keep IOCP, overlapped IO and the off-by-default state.

Status: actionable. Full: c04-d19. Partial: none.

`sources/c04.txt` bytes 13663–13727

```text
This should be much faster than using event-based
  notification
```

## exposed_action / removed

### 3: filler.evaluative-closure

Row counts is a noun in a list of measured report fields, not a subject claiming that a row matters. This is a lexical-role false positive.

Status: nonactionable. Full: none. Partial: none.

`sources/c02.md` bytes 5126–5140

```text
the row
counts
```

## confirmation / added

### 11: filler.unscoped-assurance

Slow to accept connections specifies the operational symptom for increasing the connection timeout. It is a conditional restriction on databases, not a bare quality endorsement. The matcher misreads a relative predicate as the whole sentence judgment.

Status: nonactionable. Full: none. Partial: none.

`sources/c02.md` bytes 13811–13886

```text
Raise `--connect-timeout` for databases that are slow to accept connections
```

## confirmation / removed

### 22: filler.instruction-scaffolding

The relative can-be-used support chain identifies the frozen configure-options scaffold. Preserve the embedded-library size goal and the explicit compiler/feature options. Its removal loses this full detection.

Status: actionable. Full: c04-d13. Partial: none.

`sources/c04.md` bytes 15155–15308

```text
There are a number of configure options that can be used to reduce the size of
libcurl for embedded applications where binary size is an important factor
```
