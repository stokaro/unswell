# Frozen confirmation defects still missed

Both profiles miss the same 87 events. IDs and byte ranges refer to the frozen
confirmation archive; full context and proposed edits remain in annotations.json.

## c02-d01: vague_claims

The opening criticizes unspecified database tools without a comparison or example; the definitions do not need that claim.

Proposed edit: Start with the dialect and database-engine definitions.

Bytes 507–565:

```text
Ptah keeps two ideas apart that database tools often blur.
```

## c02-d02: unjustified_intensifiers

The moral evaluation adds no technical property to the following capability-based explanation.

Proposed edit: Remove honestly and retain the preset behavior.

Bytes 3188–3196:

```text
honestly
```

## c03-d01: empty_framing

The phrase announces the precision and worth of the definition before stating it.

Proposed edit: State directly that every verb requires these fields and exits 2 when one is missing.

Bytes 1160–1216:

```text
What the mark says
is narrower and worth having exactly:
```

## c03-d02: empty_framing

The worth announcement adds no filter behavior; the following sentence already explains catch-up.

Proposed edit: Keep that filter narrows backfill and catch-up, then state both effects.

Bytes 2509–2545:

```text
and the second half is worth stating
```

## c03-d03: wordiness

The closing evaluation repeats the preceding type-mismatch consequence instead of giving the check directly.

Proposed edit: Check that version_field has the type required by version_strategy; keep the late-retry consequence above.

Bytes 4490–4610:

```text
so this is a configuration error with a silent cost, and the pairing is
worth checking when you write the specification.
```

## c03-d04: wordiness

The worth-being construction obscures the identity rule with an abstract judgment.

Proposed edit: Say the identity includes endpoint_class to distinguish provider trust boundaries.

Bytes 8023–8101:

```text
because a change of trust boundary is a change worth being a different corpus.
```

## c03-d05: wordiness

The narrative about the author delays the explicit-empty versus absent-field distinction.

Proposed edit: State that an explicit empty layout is invalid, while omission selects source_columns.

Bytes 10360–10483:

```text
because a template that filled in nothing is the one spelling of this key that
means an author tried to choose and did not.
```

## c03-d06: empty_framing

The closing personification restates what the minimum-row guard declares without giving a new condition.

Proposed edit: Remove the sentence or give the direct instruction to set a positive minimum for a nonempty corpus.

Bytes 12258–12330:

```text
This is how an environment that knows its corpus is never empty
says so.
```

## c04-d01: wordiness

The worth announcement delays the concrete type-mapping statement.

Proposed edit: State that Oracle maps BOOLEAN and enum through the listed representations, retaining the lack of direct equivalents.

Bytes 2111–2187:

```text
Two type mappings are worth knowing because Oracle has no direct equivalent:
```

## c04-d02: needless_complexity

The nested shape description and unclear which antecedent obscure the refusal policy.

Proposed edit: State that Ptah retains the server refusal instead of forcing a change that would invalidate a dependent column.

Bytes 4610–4718:

```text
the server declining to leave that column
naming a shape it no longer has, which is kept rather than forced.
```

## c05-d01: wordiness

The worth announcement delays the two directory exceptions without adding a condition.

Proposed edit: State the checkpoint and down exceptions directly.

Bytes 4643–4691:

```text
and the difference is worth knowing before a run
```

## c05-d03: wordiness

The machine-versus-reading phrase adds no useful distinction after naming a pipeline as the parser.

Proposed edit: Say that pipelines parse the status output, then retain the exact compatibility fields and native-output distinction.

Bytes 8939–8971:

```text
with a machine rather than reads
```

## c05-d04: empty_framing

The announcement of different answers adds nothing before the two explicit scope definitions.

Proposed edit: End the lead-in after what the URL pins, then give both scope cases.

Bytes 21858–21892:

```text
and the two answers are different.
```

## c05-d05: wordiness

The general per-dialect-fact rationale repeats that the default is family-specific; the concrete MySQL and SQLite distinctions supply the explanation.

Proposed edit: Keep the family-scoped default and the concrete MySQL and SQLite storage rules without the abstract rationale.

Bytes 25192–25272:

```text
The default is scoped to that family because the location is a per-dialect
fact.
```

## c05-d06: empty_framing

The importance announcement interrupts a concrete cleanup-order explanation.

Proposed edit: Connect the pre-connect refusal directly to the disposable-database cleanup consequence.

Bytes 60449–60480:

```text
which is the part that matters:
```

## c05-d07: needless_repetition

The repeated separate/two formulation restates independence without naming an additional requirement.

Proposed edit: Say that both flags are required and each missing flag has its own refusal.

Bytes 59332–59401:

```text
and they are **two separate
requirements** with two separate refusals
```

## c05-d08: needless_complexity

The command line answers a sentence metaphor obscures which missing-argument error takes precedence.

Proposed edit: State that when both arguments are missing, Ptah reports the missing dev URL first, matching the pinned Atlas binary.

Bytes 59700–59822:

```text
An argv missing both answers the `--dev-url` sentence, because that is the one
the pinned binary answers on the same argv.
```

## c05-d09: vague_claims

The universal truth claim overstates the preceding, specifically bounded over-reporting behavior.

Proposed edit: Keep the exact three over-reported constructs and exit status; omit the universal truth claim.

Bytes 68771–68840:

```text
so a
scoped run is never told less than the truth about its database.
```

## c05-d10: empty_framing

This explains the decision to document the cases after already documenting them.

Proposed edit: Remove the sentence; retain the measured differences and source scope.

Bytes 68841–68896:

```text
They are listed
here rather than left to be discovered.
```

## c05-d11: wordiness

Two lengthy reason introductions and a closing appeal to the lack of another tool bury the actual distinction between diff-based compatibility analysis and native SQL-text analysis.

Proposed edit: State the two analysis models directly, then preserve the TRUNCATE, DROP SCHEMA and scoped DROP TABLE examples and their outcomes.

Bytes 69104–69808:

```text
The reason the boundary exists on the compatibility surface is that the tool it
replaces reviews only what the dev URL covers, and matching that is the whole
point of the surface. The reason it does not exist natively is that the
justification for it — an object outside the dev URL's reach was never in the
before-state the run compares against — describes a diff-based analyzer, and
Ptah's linter reads SQL text. That is why it reports `TRUNCATE` and
`DROP SCHEMA`, neither of which produces a diff. Scoped natively,
`DROP TABLE app."Users", app.audit_log;` under `search_path=public` reports
nothing and exits 0, on the one surface where no other tool can be consulted
about whether that is right.
```

## c05-d12: empty_framing

The slogan restates the preceding worked comparison without a new instruction or consequence.

Proposed edit: End after the measured checkpoint, apply and new results.

Bytes 83502–83572:

```text
One binary, two rules, on the one
directory shape that separates them.
```

## c05-d13: wordiness

The personified argument and unspecified comparative safety judgment delay the actual monotonic version rule.

Proposed edit: State that this Ptah-only layout keeps monotonically increasing versions, preserving the ten-digit overflow refusal.

Bytes 85201–85342:

```text
nothing outside Ptah reads
that layout, so no compatibility argument moves it, and a version that only goes
forwards is the safer of the two.
```

## c05-d14: wordiness

The metaphor restates that Ptah can reject drift even though the community command is unsupported.

Proposed edit: Remove the metaphor; retain the unsupported-command distinction and the two concrete rollback report paths.

Bytes 90854–90935:

```text
and "matching is the floor, not the ceiling" is what leaves room
for the refusal.
```

## c05-d15: needless_complexity

The pronoun follows several paragraphs about several verbs and no longer identifies the command to which the project defaults apply.

Proposed edit: Name the intended command explicitly before listing the environment defaults; do not infer its identity from neighboring flag names.

Bytes 92728–92750:

```text
With `--env`, it reads
```

## c05-d16: wordiness

The contrast introduces an unavailable action after already saying successful import is silent.

Proposed edit: Tell readers to inspect the destination directory and checksum; retain the stderr error behavior.

Bytes 93780–93830:

```text
rather than reading a progress
message off stdout.
```

## c05-d17: empty_framing

The purpose restatement adds no exception to the missing-checksum import rule.

Proposed edit: Keep the unhashed external-directory explanation and omit the purpose restatement.

Bytes 95663–95705:

```text
and importing it is what
this verb is for.
```

## c06-d01: empty_framing

The link-utility explanation restates the immediately preceding statement that Partial links the owning issue.

Proposed edit: Keep the status meaning and issue link without explaining that readers can follow it.

Bytes 1022–1075:

```text
so the field points at something a reader can follow.
```

## c06-d02: needless_complexity

The spatial metaphor and opposite-answer phrase obscure the actual classification difference.

Proposed edit: State that Native means Ptah implements a capability that Atlas restricts to Pro or Cloud, while Out of scope means Ptah does not target it.

Bytes 3423–3526:

```text
It sits beside Out of scope rather than inside it: the same Atlas gating, and the opposite answer here.
```

## c06-d03: needless_repetition

The identity repeats the earlier strict-supported-subset statement without adding a limitation.

Proposed edit: Keep the link to the attribute-level support reference.

Bytes 18070–18099:

```text
The subset is still a subset,
```

## c06-d04: empty_framing

The document-maintenance justification is unnecessary after providing the detailed reference.

Proposed edit: End with the attribute-level reference link.

Bytes 18193–18244:

```text
rather than this page restating a list that drifts.
```

## c06-d06: needless_complexity

The nested it/one references make the rollback-loss warning hard to follow.

Proposed edit: State that stderr names each source file whose rollback section was not imported.

Bytes 33842–33902:

```text
the import names each source file it left one in, on stderr,
```

## c06-d07: empty_framing

The sentence announces an omission after the references already identify the authoritative lists.

Proposed edit: Remove it and retain both source references.

Bytes 39852–39883:

```text
Neither count is repeated here.
```

## c06-d08: wordiness

The abstract what-keeps frame and negative contrast obscure the evidence limitation.

Proposed edit: State that attribute-level parity has not been measured and no specific missing attribute is identified here.

Bytes 40827–40967:

```text
What keeps this Partial is that the remaining gap has not been measured attribute by attribute rather than any named attribute being absent.
```

## c07-d01: unjustified_intensifiers

The adverb adds no classification criterion to the three contribution methods.

Proposed edit: Remove fundamentally.

Bytes 1048–1061:

```text
fundamentally
```

## c07-d02: needless_repetition

Opening an issue for discussion is restated as creating a new issue being the way to report a bug.

Proposed edit: Say to report a suspected bug in the linked issue tracker, retaining the example status.

Bytes 1107–1310:

```text
By opening the issue for discussion: For instance, if you believe that you
   have discovered a bug in Tokio, creating a new issue in [the tokio-rs/tokio
   issue tracker][issue] is the way to report it.
```

## c07-d03: wordiness

The method announcement This can be done by repeats the triage action before naming the actual contributions.

Proposed edit: List adding a reproducing test, suggesting a fix and checking tags as triage actions.

Bytes 1315–1542:

```text
By helping to triage the issue: This can be done by providing
   supporting details (a test case that demonstrates a bug), providing
   suggestions on how to address the issue, or ensuring that the issue is tagged
   correctly.
```

## c07-d04: wordiness

The typically-is-done-in-the-form-of frame and changes-some-bit-of-something description obscure the two concrete resolution paths.

Proposed edit: Explain either why the reported behavior is not a bug or open a reviewable PR to fix it.

Bytes 1547–1821:

```text
By helping to resolve the issue: Typically this is done either in the form of
   demonstrating that the issue reported is not a problem after all, or more
   often, by opening a Pull Request that changes some bit of something in
   Tokio in a concrete and reviewable manner.
```

## c07-d05: needless_repetition

The adjacent sentences repeat the instruction to fill out the new-issue template.

Proposed edit: Ask once to fill out the issue template and retain reassurance that incomplete details are acceptable.

Bytes 2436–2683:

```text
When opening a new issue in the Tokio issue tracker, you will be presented
with a basic template that should be filled in. If you believe that you have
uncovered a bug, please fill out this form, following the template to the best
of your ability.
```

## c07-d06: wordiness

The impersonal double negative announces an ordinary discussion without adding a condition.

Proposed edit: Start with contributors having different opinions, then keep the conduct guidance.

Bytes 3339–3426:

```text
Once an issue has been opened, it is not uncommon for there to be discussion
around it.
```

## c07-d07: wordiness

The empowerment clause repeats mutual help in abstract terms.

Proposed edit: Ask contributors to help each other resolve issues; retain the concrete explanation and reconsideration guidance.

Bytes 3835–3981:

```text
Contributors are encouraged to help one another make forward progress as much as
possible, empowering one another to solve issues collaboratively.
```

## c07-d08: wordiness

The enumeration and anaphoric restart delay a test requirement applying to code changes.

Proposed edit: State that PRs changing code should include regression tests, retaining the distinction from documentation-only changes and the preferred test types.

Bytes 6397–6604:

```text
If the change being proposed alters code (as opposed to only documentation for
example), it is either adding new functionality to Tokio or it is fixing
existing, broken functionality. In both of these cases,
```

## c07-d09: wordiness

The given-that introduction and closing this-is-how-it-is-structured obscure the specific API choice being explained.

Proposed edit: State that this type-level example uses FutureExt::timeout because users normally construct Timeout through that method.

Bytes 8885–9095:

```text
Given that this is a *type* level documentation test and the primary way users
of `tokio` will create an instance of `Timeout` is by using
`FutureExt::timeout`, this is how the documentation test is structured.
```

## c07-d10: wordiness

The recommendation-of-a-best-practice frame duplicates its advisory force.

Proposed edit: Recommend grouping each logical change into a commit; keep the guidance about multiple commits.

Bytes 9868–9982:

```text
It is a recommended best practice to keep your changes as logically grouped as
possible within individual commits.
```

## c07-d11: needless_repetition

The closing process justification repeats the preceding explanation of review feedback instead of adding a contributor action.

Proposed edit: Keep the feedback expectations and reassurance; omit the final process justification.

Bytes 12582–12689:

```text
This is a necessary part of the process
in order to evaluate whether the changes are correct and necessary.
```

## c07-d12: needless_repetition

The definition repeats its own subject without distinguishing what is categorized.

Proposed edit: Use the Category heading alone or define the type of issue or contribution that the labels classify.

Bytes 18476–18518:

```text
The category label describes the category.
```

## c08-d01: empty_framing

The broad trend and task-importance introduction supplies no curl operation, constraint or prerequisite beyond the following guide.

Proposed edit: Begin with the supported scripting tasks or the existing curl introduction without asserting a trend or their general importance.

Bytes 143–412:

```text
 The increasing amount of applications moving to the web has made "HTTP
 Scripting" more frequently requested and wanted. To be able to automatically
 extract information from the web, to fake users, to post or upload data to
 web servers are all important tasks today.
```

## c08-d02: needless_repetition

Getting data and retrieving information repeat the same role in a list of curl operations.

Proposed edit: Say curl sends requests and transfers data; retain that scripts coordinate the operations.

Bytes 762–851:

```text
It makes the requests, it gets
 the data, it sends data and it retrieves the information.
```

## c08-d03: unjustified_intensifiers

The simplicity judgment is unsupported and not needed to identify HTTP and its transport.

Proposed edit: Remove the simplicity judgment; keep the protocol description.

Bytes 1053–1064:

```text
very simple
```

## c08-d04: unjustified_intensifiers

The universal ranking of the debugging option has no stated comparison or task boundary.

Proposed edit: Recommend verbose output for observing the interaction without ranking every alternative.

Bytes 2075–2093:

```text
single most useful
```

## c08-d05: wordiness

The reader-wondering scenario and for-those-and-other-similar-situations bridge delay the concrete timing option.

Proposed edit: State that trace-time prefixes trace lines with timestamps for measuring intervals during a transfer.

Bytes 2531–2870:

```text
 Many times you may wonder what exactly is taking all the time, or you just
 want to know the amount of milliseconds between two points in a transfer. For
 those, and other similar situations, the
 [`--trace-time`]((https://curl.se/docs/manpage.html#--trace-time) option
 is what you need. It'll prepend the time to each trace output line:
```

## c08-d06: unjustified_intensifiers

The familiarity claim and hyperbole add no URL syntax or reference information.

Proposed edit: Keep the definition, examples if useful, and the RFC reference without presuming reader familiarity.

Bytes 3225–3320:

```text
You know these, you've seen URLs like
 https://curl.se or https://yourbank.com a million times.
```

## c08-d07: empty_framing

The emotional evaluation adds no workaround or diagnostic detail to HEAD being refused.

Proposed edit: Keep the method-refusal caveat and omit the annoyance judgment.

Bytes 6464–6504:

```text
which is a particular kind of annoyance.
```

## c08-d08: needless_repetition

The short emphatic fragments repeat the preceding any-amount claim.

Proposed edit: State the multiple-URL behavior once and retain the request-per-URL consequence.

Bytes 6967–6987:

```text
Yes
 any. No limits.
```

## c08-d09: wordiness

The separator/separates predicate repeats the relational noun and adds a vague object.

Proposed edit: State that next separates successive option groups, preserving the method and POST-data scope.

Bytes 7659–7736:

```text
It is basically
 a separator that separates a bunch of options from the next.
```

## c08-d10: vague_claims

The hedge makes the specified reset behavior imprecise without naming a limit.

Proposed edit: State which method and POST data reset, or name any actual exception.

Bytes 7901–7914:

```text
sort of reset
```

## c08-d11: empty_framing

The example-worth announcement is unnecessary before the explicit HEAD/GET example purpose.

Proposed edit: Start with To send first a HEAD and then a GET.

Bytes 7968–8015:

```text
Perhaps this is best shown with a few examples.
```

## c08-d12: wordiness

The metaphor repeats the need for a receiving server program without specifying an additional requirement.

Proposed edit: Retain that the server needs a program to receive the submitted data.

Bytes 8880–8928:

```text
You cannot just invent something out of the air.
```

## c08-d13: wordiness

The paragraph presumes the reader is `of course very interested` and repeats the task before showing how to inspect the browser request.

Proposed edit: Say to reproduce the browser POST when submitting a form with curl, then give the inspection method.

Bytes 13017–13195:

```text
 When you're about fill in a form and send to a server by using curl instead
 of a browser, you're of course very interested in sending a POST exactly the
 way your browser does.
```

## c08-d15: unjustified_intensifiers

The insult targets authors rather than specifying the behavior or its limitations.

Proposed edit: Use applications or sites and retain the browser-specific response behavior.

Bytes 17037–17059:

```text
Silly web
 programmers
```

## c08-d16: empty_framing

The unnamed-option announcement repeats what the next instruction and example immediately specify.

Proposed edit: Keep the default behavior and give the location option directly.

Bytes 18246–18343:

```text
It does however
 feature an option that will make it attempt to follow the `Location:`
 pointers.
```

## c08-d17: unjustified_intensifiers

The vague intensifier adds no cookie parsing capability to the concrete reuse description.

Proposed edit: Remove full blown; keep parsing and reuse behavior.

Bytes 20509–20519:

```text
full blown
```

## c08-d18: vague_claims

The quantity and advancement claims name no feature or mechanism and repeat the encryption topic.

Proposed edit: Name the relevant TLS functions if needed or omit this general feature claim.

Bytes 22212–22343:

```text
offers a
 truckload of advanced features to allow all those encryptions and key
 infrastructure mechanisms encrypted HTTP requires.
```

## c08-d19: empty_framing

The noticing instruction adds no condition to curl choosing a request method.

Proposed edit: State the automatic method selection directly.

Bytes 24653–24676:

```text
It should be noted that
```

## c08-d20: wordiness

The can-also-be-noted frame, properly-in-an-automated-fashion phrase and most-certainly hedge delay the scripting requirement.

Proposed edit: Say automated login usually requires a script and multiple curl calls, preserving the nonuniversal qualification.

Bytes 25618–25770:

```text
 It can also be noted that to do this properly in an automated fashion, you
 will most certainly need to script things and do multiple curl invokes etc.
```

## c09-d02: wordiness

The impersonal possibility construction pads two concrete supported actions.

Proposed edit: Say You can use different link types or attach text to a link.

Bytes 3739–3821:

```text
It is possible to have different types of links or attach a text string to a link.
```

## c09-d03: wordiness

The impersonal capability frame and as-per-below reference delay the declaration instruction.

Proposed edit: Say You can declare several links on one line, as shown below.

Bytes 4811–4875:

```text
It is possible declare many links in the same line as per below:
```

## c09-d05: unjustified_intensifiers

The praise does not state what additional dependency structure the syntax supports.

Proposed edit: Introduce the one-line dependency example without the expressive judgment.

Bytes 5186–5210:

```text
in a very expressive way
```

## c09-d07: wordiness

The vocabulary detour restates the warning not to make the graph source hard to read.

Proposed edit: Keep the four-line comparison and the readability warning; omit the detour.

Bytes 5505–5630:

```text
The Swedish word `lagom` comes to mind. It means, not too much and not too little.
This goes for expressive syntaxes as well.
```

## c09-d08: wordiness

The existence-of-possibility frame delays the available action.

Proposed edit: Say flowcharts support multidirectional arrows, retaining the graph-versus-flowchart condition.

Bytes 5991–6047:

```text
there is the possibility to use multidirectional arrows.
```

## c09-d11: wordiness

The capability introduction pads a direct statement about edges; the graph-type prerequisite remains meaningful.

Proposed edit: Keep the flowchart graph-type condition and say You can connect edges to and from subgraphs.

Bytes 9280–9357:

```text
it is also possible to set edges to and from subgraphs as in the graph below.
```

## c09-d14: wordiness

The unnamed-other-way announcement delays the concrete ordering mechanism and repeats the lack of IDs.

Proposed edit: State that links have no IDs, so their declaration order selects which link receives a style; retain the fourth-link example.

Bytes 12744–12957:

```text
As links
have no ids in the same way as nodes, some other way of deciding what style the links should be attached to is required.
Instead of ids, the order number of when the link was defined in the graph is used.
```

## c09-d15: wordiness

The impersonal possibility frame adds no condition to the node-style capability.

Proposed edit: Say You can change a node border or background color, keeping the stated examples.

Bytes 13152–13259:

```text
It is possible to apply specific styles such as a thicker border or a different background color to a node.
```

## c09-d16: wordiness

The nominalized attachment and is-done-as-per-below frame delay the action.

Proposed edit: Introduce the example as Attach a class to a node, or You can attach a class as shown below.

Bytes 13926–13979:

```text
Attachment of a class to a node is done as per below:
```

## c09-d18: wordiness

The possibility introduction and embedded applied-from clause pad a direct CSS capability.

Proposed edit: Say You can predefine CSS classes and apply them in the graph definition; keep the example reference.

Bytes 14428–14553:

```text
It is also possible to predefine classes in css styles that can be applied from the graph definition as in the example
below:
```

## c09-d20: needless_repetition

The old-syntax-still-valid assurance is repeated around the same new syntax example without another compatibility condition.

Proposed edit: State once that old declarations remain valid; separately preserve the semicolon version and spacing restrictions.

Bytes 15626–15872:

```text
In graph declarations, the statements also can now end without a semicolon. After release 0.2.16, ending a graph statement with semicolon is just optional. So the below graph declaration is also valid along with the old declarations of the graph.
```

Bytes 16026–16156:

```text
The old syntax of graph declaration will also work and hence this new feature is optional and is introduce to improve readability.
```

Bytes 16158–16276:

```text
Below is the new declaration of the graph edges which is also valid along with the old declaration of the graph edges.
```

## c09-d21: wordiness

The anaphoric doing frame obscures the two ways to set the width mentioned in the preceding question.

Proposed edit: Name configuring width through mermaid.flowchartConfig or a CLI JSON file, preserving the alternative mechanisms and value forms.

Bytes 16658–16767:

```text
This is done by defining **mermaid.flowchartConfig** or by the CLI to use a json file with the configuration.
```

## c10-d01: unjustified_intensifiers

The obviousness assertion judges reader knowledge without explaining the scalar operation.

Proposed edit: State the scalar result directly.

Bytes 608–632:

```text
the behavior is obvious:
```

## c10-d02: vague_claims

The caution does not identify a risk or what readers should check; advanced and carefully do not provide a decision criterion.

Proposed edit: State the matching requirement or relevant cardinality risk, retaining ignoring as the simpler alternative when applicable.

Bytes 7773–7869:

```text
Many-to-one and one-to-many matching are advanced use cases that should be carefully considered.
```

## c10-d03: wordiness

The reader-interest condition and we-could-simply-write frame delay the concrete total across all applications.

Proposed edit: Introduce the example as To total HTTP requests across all applications, use the following query.

Bytes 10855–10971:

```text
If we are just interested in the total of HTTP requests we have seen in **all**
applications, we could simply write:
```

## c11-d01: formulaic_transitions

The stock transition adds no order requirement to the initial formatting example.

Proposed edit: Start with the italics example.

Bytes 406–427:

```text
First thing is first.
```

## c11-d02: unjustified_intensifiers

The ease judgment adds no formatting instruction and presumes reader difficulty.

Proposed edit: State that a blank line separates paragraphs.

Bytes 570–601:

```text
Paragraphs are pretty easy too.
```

## c11-d03: wordiness

The reader-desire condition pads the instruction while the preserved-linebreak requirement can remain a concise purpose clause.

Proposed edit: Say To include code with preserved line breaks, indent with a tab or at least four spaces.

Bytes 982–1091:

```text
If you want to include code and have new
lines preserved, indent the line with a tab
or at least four spaces:
```

## c11-d04: wordiness

The repeated need construction delays the action without introducing a state prerequisite.

Proposed edit: Say To insert a horizontal rule, put at least three of the specified characters on their own line; preserve the character and spacing conditions.

Bytes 5018–5068:

```text
If you need a horizontal rule you just need to put
```

## c12-d01: empty_framing

The general importance claim justifies having a scope document without stating a project boundary.

Proposed edit: Start with the criteria used to review proposed project changes.

Bytes 24–116:

```text
Having a clearly defined scope of a project is important for ensuring consistency and focus.
```

## c12-d02: vague_claims

The intended component interaction is described through an undefined quality metaphor rather than an observable API property.

Proposed edit: Specify the desired compatibility of the APIs, using the shared Mount contract in the following example.

Bytes 473–492:

```text
have a natural flow
```

## c12-d03: wordiness

The example-can-be-seen frame delays the concrete pair of components.

Proposed edit: Name the overlay filesystem and execution layer as the example, preserving their independent use and shared Mount value.

Bytes 529–572:

```text
An example for this design can be seen with
```

## c12-d04: needless_repetition

The two consecutive table-specifies sentences can state the table purpose once; the second only adds the scope column.

Proposed edit: Say the table lists container-runtime components and whether each is in scope.

Bytes 2086–2276:

```text
The following table specifies the various components of containerd and general features of container runtimes.
The table specifies whether or not the feature/component is in or out of scope.
```
