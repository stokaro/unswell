# Reviewed diagnostic changes

Indices refer to retained strict reports. Both profiles have complete saved dispositions.

## exposed_repetition: added 41 (filler.evaluative-closure)

Status: additional_actionable. Full frozen credit: none.

The detached regret cue can be removed while preserving the slower-build observation and the immediately following timing measurements. This tone edit was not in the old targeted labels.

sources/c06.md bytes 22310–22359:

> Unfortunately, it's slower in my build of ripgrep

## exposed_framing: added 118 (filler.evaluative-closure)

Status: additional_actionable. Full frozen credit: none.

The mouse-wheel regression is a real consequence. Remove only the detached regret cue; preserve that consequence and the workaround. The earlier control concerns the separate small-file convenience explanation.

sources/c09.md bytes 15177–15236:

> Unfortunately, it also breaks mouse-wheel support in `less`

## exposed_scope: added 69 (filler.evaluative-closure)

Status: actionable. Full frozen credit: c04-d25.

The reader-obviousness cue matches the frozen emphasis defect. Preserve curl_multi_timeout compatibility and the existing API citations.

sources/c04.md bytes 31569–31732:

> Of course, the upside of all this is that we get a [`curl_multi_timeout()`][8] that should also work with old-style applications that use [`curl_multi_perform()`

## exposed_scope: added 78 (filler.evaluative-closure)

Status: actionable. Full frozen credit: c06-d01.

The assumed-obviousness cue is exactly the frozen defect. The arbitrary nesting-depth behavior must remain.

sources/c06.md bytes 6392–6442:

> Of course, the same can be done at any depth level

## exposed_proposition: added 18 (filler.instruction-scaffolding)

Status: actionable. Full frozen credit: c05-d07.

The invitation announces the example execution without another prerequisite. Keep the following code and output.

sources/c05.md bytes 7149–7171:

> Let's see it in action

## exposed_action: added 29 (filler.evaluative-closure)

Status: additional_actionable. Full frozen credit: none.

The detached regret cue adds no backend constraint. Preserve the select-based implementation and the following IOCP explanation.

sources/c04.txt bytes 14122–14185:

> Unfortunately, the main Windows backend is still select()-based

## exposed_boundaries: added 29 (filler.evaluative-closure)

Status: actionable. Full frozen credit: c04-d09.

The detached interest judgment is the frozen defect. The entire glob-order condition and explanation stay in the result.

sources/c04.md bytes 12737–12954:

> Interestingly, reversing the order of the globs in this case will match nothing, since the presence of at least one non-blacklist glob will institute a requirement that every file searched must match at least one glob

## exposed_boundaries: added 39 (filler.evaluative-closure)

Status: actionable. Full frozen credit: c04-d17.

The diagnostic identifies the example-purpose announcement, including its assumed-obviousness cue. Keep the concrete raw-byte demonstration and simpler command.

sources/c04.md bytes 26387–26471:

> Of course, that's just an example meant to show how one can drop down into raw bytes

## exposed_grammar: added 47 (filler.instruction-scaffolding)

Status: actionable. Full frozen credit: c06-d02.

The invitation merely introduces the immediately following expression-browser instructions, as the frozen review records.

sources/c06.md bytes 2631–2693:

> Let us explore data that Prometheus has collected about itself

## exposed_grammar: added 48 (filler.instruction-scaffolding)

Status: actionable. Full frozen credit: c06-d04.

The future narrator repeats the section action before the actual target-grouping instructions. Keep the grouping and scrape configuration.

sources/c06.md bytes 4956–5016:

> Now we will configure Prometheus to scrape these new targets

## confirmation: added 60 (filler.evaluative-closure)

Status: uncertain. Full frozen credit: none.

The only new confirmation warning overlaps c09-u01: explicit author regret in a motivations section followed by concrete library limitations. It remains uncertain, without frozen-defect credit.

sources/c09.md bytes 459–562:

> Unfortunately, the state of the Go client libraries for Kafka at the time of this writing was not ideal
