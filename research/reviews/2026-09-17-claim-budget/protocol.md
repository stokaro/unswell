# Repeated-claim applicability on long documents

Issue #312 records a budget abstention on the pinned Ptah migration reference.
The raised-budget control finishes with no repeated-claim findings. This work
addresses avoidable identity-construction work, not missed rhetorical patterns.

The old implementation formats every token into a quoted key and separately
walks each byte of the source map. Build a length-delimited key while walking
that map. Keep exact token boundaries, protected markers, case, punctuation,
opaque code operands and mapping exclusions. Preserve candidate eligibility,
window ownership, observations and finding evidence. Charge actual work without
raising the default limit. Real exhaustion and cancellation remain explicit.

Replay all 82 retained pages from the construction-recall review under unchanged
technical and strict policies. Compare every completed report, finding, source
range, assessment and gate. The sole intended applicability difference is that
the long migration reference no longer exhausts its repeated-claim budget.
Do not alter frozen sources, annotations or editorial credits. If matching
semantics differ, investigate and report the difference before publication.

Also compare block activation values and ownership between the old and new
runtime with the same explicit generous budget, so the former abstention cannot
hide a candidate-observation regression. This control is not the product's
configuration. Retain commands, hashes, timing, memory and exact revisions.

A public-API regression uses the licensed frozen page, verifies its source hash,
and requires complete analysis, no abstention, and real measured activation
values at the default budget. It compares the values and source ownership with
a generous-budget run. Existing tiny-budget and cancellation controls remain.
Additional mapping controls cover entities, emphasis, links, Unicode, opaque
operands, case and whitespace. Run the required checks and CLI/MCP dogfood;
race, active fuzzing and coverage remain deferred to #123.
