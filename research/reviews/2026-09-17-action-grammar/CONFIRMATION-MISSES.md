# Remaining confirmation defects

All 51 lack full coverage in both profiles. No partial event receives full credit.

## c02-d01: vague_claims

The ease-of-confusion and cost judgments give no reader context or cost condition before the concrete definitions.

Proposed edit: Introduce verification, cutover and rollback directly, retaining their different effects.

Bytes 545–615

```text
Three operations are easy to confuse, and confusing them is expensive.
```

## c02-d02: empty_framing

The page announcement repeats the immediately following three definitions.

Proposed edit: Keep the definitions without announcing the page.

Bytes 616–641

```text
This page
separates them.
```

## c02-d03: wordiness

The names/checked/checks-everything chain restates the need to disclose unmeasured layers indirectly.

Proposed edit: State that the report identifies unmeasured layers; retain the dimension-without-value-read example.

Bytes 2034–2144

```text
and says so rather than letting a
report that names only what it checked read as though it checked everything.
```

## c02-d04: empty_framing

The attention announcement adds no limit to the explicit verified-condition list.

Proposed edit: Keep the exact generation and condition list without the worth-stating announcement.

Bytes 2435–2460

```text
and worth stating in full
```

## c02-d05: wordiness

The name/name/wrote-down phrasing indirectly defines an operator assertion.

Proposed edit: State that this authorization records an operator-supplied name, and retain the distinction from a signature and approval_signed.

Bytes 3198–3254

```text
A name given this way is a name the operator wrote down.
```

## c02-d06: wordiness

The weight metaphor postpones the precise accepted-findings distinction.

Proposed edit: Name the accepted blocking findings directly, retaining why they must be in the signed plan.

Bytes 4620–4693

```text
The
acceptance of blocking findings is the line that carries that weight:
```

## c02-d07: wordiness

The author/reader/tell scenario repeats the ambiguity of an omitted empty list.

Proposed edit: Say that an explicit empty list distinguishes no accepted findings from omitted information.

Bytes 4897–5043

```text
A file silent about accepted findings cannot
be told from one whose author had nothing to say, and the reader deciding is the
one who cannot tell.
```

## c02-d08: empty_framing

The justification announces the following consequences without adding a digest condition.

Proposed edit: Present the two consequences directly.

Bytes 5212–5272

```text
Two
consequences follow, and both are why the line is there:
```

## c02-d09: empty_framing

The citation-versus-number judgment repeats the already stated recomputability without another verification step.

Proposed edit: Keep that a holder of the record can recompute the digest.

Bytes 5592–5647

```text
which is what makes it a citation rather than a number.
```

## c02-d10: vague_claims

The majority claim about teams has no stated evidence or population.

Proposed edit: Describe the second approach and the required ordering without the majority claim.

Bytes 6381–6413

```text
The second is what most teams do
```

## c02-d11: empty_framing

The property-versus-button slogan does not name the concrete freshness and stabilization prerequisites.

Proposed edit: Use a heading such as Rollback requirements; retain both conditions below.

Bytes 6493–6529

```text
Rollback is a property, not a button
```

## c03-d01: wordiness

The indirect something-you-did-not-ask-about clause restates the Docker credential boundary through an unnamed consequence.

Proposed edit: State that Docker-managed logins remain available; preserve the separate Ptah store boundary.

Bytes 3801–3877

```text
because taking it away would log you out
of something you did not ask about.
```

## c03-d02: wordiness

The expectation-of-the-name preface delays the exact internal-consistency limit.

Proposed edit: Link to the explanation and state the internal-consistency limit directly, retaining digest and publisher distinctions.

Bytes 6786–6889

```text
for what that verification establishes over a movable tag, which is less than
the flag's name suggests:
```

## c03-d03: wordiness

The one/one/did/somebody chain obscures the requirement to verify the index entry after upload.

Proposed edit: Say that successful manifest upload does not prove an index entry exists; retain the final check and unrepaired status.

Bytes 13832–13943

```text
because one
that accepted the manifest and built no entry looks exactly like one that did
until somebody checks
```

## c03-d04: empty_framing

The questions-and-verbs announcement repeats the structure of the following four command entries.

Proposed edit: Start with the four questions and their commands.

Bytes 14472–14546

```text
Four questions come up before an artifact is applied, and each has a verb.
```

## c03-d05: wordiness

The what/what/view-that-says chain repeats the tag-listing purpose instead of naming it directly.

Proposed edit: Say tags lists current aliases; retain the promotion-versus-pinning distinction where needed.

Bytes 14601–14740

```text
The aliases are what a
promotion moves and what a pin replaces, so this is the view that says which of
them a repository currently carries.
```

## c03-d06: empty_framing

The attention judgment adds no condition to the following durable-tag interpretation.

Proposed edit: Explain the source column directly and retain the cross-client limitation.

Bytes 15536–15584

```text
and the source column is the part worth
reading.
```

## c03-d07: wordiness

The saying-no/failure-to-ask metaphor obscures two exact outcomes.

Proposed edit: Distinguish an explicit unsupported-API response from other request errors without the conversational metaphor.

Bytes 16186–16336

```text
A refusal naming the API as unsupported is the registry saying
no; anything else is a failure to ask, and is reported as an error rather than
as a no.
```

## c03-d08: empty_framing

The testing-process claim adds no current command behavior after the flag and resolver scope is already stated.

Proposed edit: Keep the supported command scope and schema test example without narrating the CI mechanism.

Bytes 21822–21909

```text
the command-surface test keeps the flag and resolver paired as
new consumers are added.
```

## c03-d09: wordiness

Make-commands-implemented is an indirect description of the unavailable compatible verbs.

Proposed edit: State that the Atlas-compatible migrate push and schema push remain unimplemented; retain the native-versus-compatible distinction and flag behavior.

Bytes 24550–24638

```text
does not make the Atlas-compatible `migrate push` or
`schema push` implemented commands.
```

## c04-d01: wordiness

The personal certainty and joke wrap a simple incompleteness notice without specifying a missing feature. This wording was already exposed in the 2.1 notes.

Proposed edit: State that the list may be incomplete.

Bytes 278–357

```text
  NOTE: I am very sure that I missed some thing on this list.  Caveat
  haxxor.
```

## c04-d02: empty_framing

Self-evaluation and an unspecified promise add no current binary compatibility contract.

Proposed edit: Keep the required recompilation and the link to the header-layout explanation.

Bytes 1134–1276

```text
This is nothing new; we have never been good at
  preserving binary compatibility between releases.  We'll try harder in the
  future, though:
```

## c04-d03: vague_claims

The ease and code-quality claims name no outcome or condition beyond the subsequent specific header categories.

Proposed edit: Describe the regular, compatibility and structure header separation directly.

Bytes 1391–1470

```text
to make it easier for programmers to
  write good, well-supported libevent code
```

## c04-d04: vague_claims

The majority and reader-preference claims do not identify the current supported function category.

Proposed edit: Describe these as the regular API functions and preserve the example header.

Bytes 1599–1651

```text
the functions that most programmers will want to use
```

## c04-d05: vague_claims

Reader confidence is not a usable condition for depending on an unstable ABI.

Proposed edit: Keep the concrete willingness to rebuild and tolerate structure changes as the conditions; remove the knowledge judgment.

Bytes 2696–2739

```text
unless they really know what they are doing
```

## c04-d06: vague_claims

The heading promises nonspecific ease beside two concrete compatibility properties.

Proposed edit: Keep thread-safe and binary-compatible and let the explicit base and allocation mechanisms explain error prevention.

Bytes 3485–3502

```text
harder-to-mess-up
```

## c04-d07: unjustified_intensifiers

The severity adjective supplies no failure rate or additional failure mechanism.

Proposed edit: Describe the risk of attaching to the wrong base, retaining the explicit reattachment requirement.

Bytes 4052–4072

```text
horribly error-prone
```

## c04-d08: wordiness

The for-people/want/can frame surrounds a concrete alternative for two allocation goals.

Proposed edit: For stack allocation or embedding in another structure, use event2/event_compat.h; retain the version-compatibility caveat from context.

Bytes 5055–5229

```text
        (For people who do really want to allocate a struct event on the
        stack, or put one inside another structure, you can still use
        event2/event_compat.h.)
```

## c04-d10: vague_claims

The reader-reaction judgment adds no technical constraint to the lack of direct backend selection.

Proposed edit: State that explicit backend selection required environment changes.

Bytes 6784–6898

```text
This was annoying
  to programmers who wanted to pick a backend explicitly without messing
  with the environment.
```

## c04-d11: empty_framing

The effort endorsement does not explain which backends lack a feature.

Proposed edit: Keep the backend and operating-system limitations directly.

Bytes 6908–6932

```text
despite our best efforts
```

## c04-d12: vague_claims

The closing evaluation adds no detail to the need to track backend feature support.

Proposed edit: Keep the specific old selection requirement and the replacement API.

Bytes 7236–7272

```text
This tended to get quite
  ungainly.
```

## c04-d13: wordiness

The repeated application-preference framing delays the actual change to persistent timeout resetting.

Proposed edit: State that each event trigger now resets the timeout, retaining both readiness and elapsed-time trigger behavior.

Bytes 8466–8602

```text
This is not what
  applications tend to want.  Instead, applications tend to want every
  triggering of the event to re-set the timeout.
```

## c04-d14: wordiness

The reader desire and can-just-call frame delays the direct initialization instruction.

Proposed edit: To enable threading with pthreads, call evthread_use_pthreads(); keep both required libraries and their ownership.

Bytes 9543–9644

```text
If you want threading support and you're using pthreads, you can just
  call evthread_use_pthreads().
```

## c04-d15: wordiness

The reader desire and can-just-call frame surrounds the Windows-specific threading operation.

Proposed edit: To enable threading on Windows, call evthread_use_windows_threads().

Bytes 9784–9893

```text
  If you want threading support and you're using Windows, you can just
  call evthread_use_windows_threads().
```

## c04-d16: vague_claims

The ease-of-forgetting judgment is about users; the actual inconsistent-state risk can be stated directly.

Proposed edit: Describe reinitializing events without deletion as an error; preserve the requirement to enable debugging before other library calls.

Bytes 11734–11823

```text
It's been pretty easy to forget to delete all your events before you
  re-initialize them
```

## c04-d17: vague_claims

The speed comparison has neither a measurement nor a condition and is expressly speculative.

Proposed edit: Keep the epollfd-versus-socketpair change and state any performance result only when measured.

Bytes 13410–13442

```text
This is supposed to be
  faster.
```

## c04-d18: vague_claims

The universal improvement claim overstates the bounded Windows mechanisms below.

Proposed edit: Use a heading naming Windows IO and build support.

Bytes 13458–13487

```text
better support for everything
```

## c04-d19: vague_claims

The speculative magnitude has no workload or result.

Proposed edit: Keep overlapped IO and IOCP, including off-by-default status; omit the unmeasured speed promise.

Bytes 13663–13728

```text
This should be much faster than using event-based
  notification.
```

## c04-d20: empty_framing

The intelligence metaphor adds no property beyond the following explicit asymptotic lookup comparison.

Proposed edit: Start with socket-to-pending-event lookups and retain O(1) versus O(lg n).

Bytes 13964–14001

```text
Data structures are a little smarter:
```

## c04-d21: vague_claims

The general efficiency and desirability claims precede the actual copying costs and specific additions without a defined measure.

Proposed edit: Name the realloc/memmove costs and the added capabilities directly.

Bytes 14678–14754

```text
the implementation
  was very inefficient and lacked some desirable features
```

## c04-d22: empty_framing

The self-announcement duplicates the immediately following improvement sections.

Proposed edit: Proceed to the specific changes.

Bytes 14757–14804

```text
We've made many
  improvements in Libevent 2.0.
```

## c04-d23: empty_framing

The obviousness claim and terrible-interface judgment add no mechanism beyond realloc/memmove costs.

Proposed edit: Keep the old copying behavior and the new chunked design without the judgment.

Bytes 15096–15161

```text
  Needless to say, this is a terrible interface for networked IO.
```

## c04-d24: needless_repetition

Two now markers announce the same temporal change in one clause.

Proposed edit: Use one now marker or state the new evbuffer_add_cb operation directly.

Bytes 17934–17950

```text
Now, you can now
```

## c04-d27: vague_claims

The speculative future speed promise does not describe the current backend.

Proposed edit: Keep that IOCP is available but unoptimized and preserve the enablement order.

Bytes 21855–21971

```text
Supposedly, this will eventually make Windows IO much faster for
   programs using bufferevents.  We'll have to see;
```

## c04-d29: vague_claims

The proximity claim leaves the unsupported getaddrinfo features unspecified.

Proposed edit: Keep partial support explicit and identify unsupported features if known; do not invent them.

Bytes 23502–23526

```text
but it
   is quite close
```

## c04-d30: empty_framing

The obviousness aside adds nothing to the compatibility guarantee.

Proposed edit: Keep that the old evdns functions continue working.

Bytes 24159–24168

```text
of course
```

## c04-d31: wordiness

The can-more-easily announcement restates the bound TCP listener operation before the actual functions.

Proposed edit: Use evconnlistener_* in event2/listener.h to set up a bound TCP listening socket.

Bytes 24212–24386

```text
   You can now more easily automate setting up a bound socket to listen for
   TCP connections.  Just use the evconnlistener_*() functions in the
   event2/listener.h header.
```

## c04-d32: vague_claims

The degree of security has no defined threat property or scope.

Proposed edit: Name the RNG implementation and supported security properties without inventing a guarantee.

Bytes 24837–24878

```text
a fairly secure
   random stream of bytes
```

## c04-d33: vague_claims

The comparative utility claim does not say which errors are detected or how reporting differs.

Proposed edit: Describe the specific parsing error change if known; keep the separate URI parser API.

Bytes 25094–25124

```text
detect errors
   more usefully
```

## c05-d01: vague_claims

The intelligence judgment obscures the documented operation and supplies no derivation rule.

Proposed edit: State that absent derives the output labels from the input in these examples; retain the example scope.

Bytes 956–995

```text
tries to be smart about deriving labels
```

## c05-d02: vague_claims

The intelligence judgment does not specify label derivation for absent_over_time.

Proposed edit: Describe the derivation for these examples directly without claiming intelligence.

Bytes 1677–1716

```text
tries to be smart about deriving
labels
```
