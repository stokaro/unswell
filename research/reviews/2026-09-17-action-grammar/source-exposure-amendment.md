# Source-exposure amendment

Recorded after source-only reading and before runtime changes or diagnostic output.
The original selection and protocol remain frozen. Libevent 2.0 c04 repeats a
paragraph in the already exposed Libevent 2.1 c11 notes. Different source paths
and hashes did not prevent this template overlap.

Keep all six selected pages and all 57 frozen defects. Report c04 and the other
five pages separately. The latter contain 24 defects; this is a small confirmation
subset, not a population estimate or proof of independent provenance. Do not
replace the inconvenient selected page or count c04 as wholly unexposed.

The source-only audit compared each complete nonexcluded extracted block of at
least 12 whitespace tokens with all 124 prior sources after whitespace
normalization. Its one exact match and source locations are in
`confirmation/source-overlap.json`. Shorter, partial, differently formatted and
paraphrased overlap can escape this check. Absence of a match is not a novelty
guarantee. The audit does not infer human authorship.

`tools/check_source_overlap.py --packets DIR` reproduces the comparison from
source-only extractor packets named cNN.json plus the archived source basenames.
It prints evidence and does not overwrite the frozen record.
