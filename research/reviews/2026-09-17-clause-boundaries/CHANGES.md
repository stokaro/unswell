# Reviewed changes

Indices refer to retained strict reports. Exact before and after judgments remain in dispositions.json.

## exposed_local / removed

### 73: filler.unscoped-assurance

The surrounding paragraph states a coarser clock-resolution tradeoff and provides a precise-timer opt-out. The warning isolates its invocation-cost comparison from that technical context; absence of a numeric benchmark alone does not establish an editorial defect here.

Status: nonactionable. Full: none. Partial: none.

`sources/c11.txt` bytes 25151–25202

```text
and is much faster
  to invoke than CLOCK_MONOTONIC
```

## exposed_clauses / added

### 34: filler.instruction-scaffolding

The named utility function and can-be-used/apply-logic chain identify the frozen indirect API description. Keep parse_obj_as, arbitrary types and the model-bound contrast.

Status: actionable. Full: c04-d19. Partial: none.

`sources/c04.md` bytes 21478–21638

```text
Pydantic includes a standalone utility function `parse_obj_as` that can be used to apply the parsing
logic used to populate pydantic models in a more ad-hoc way
```

## exposed_roles / added

### 22: filler.instruction-scaffolding

The restored relative configuration-option action identifies the frozen existential/can-be-used scaffold. Preserve binary-size scope and the explicit compiler options.

Status: actionable. Full: c04-d13. Partial: none.

`sources/c04.md` bytes 15155–15308

```text
There are a number of configure options that can be used to reduce the size of
libcurl for embedded applications where binary size is an important factor
```

### 27: filler.unscoped-assurance

The local process-is-simple judgment is the frozen unsupported ease claim; preserve the introductory secrets-use goal and actual configuration steps.

Status: actionable. Full: c05-d06. Partial: none.

`sources/c05.md` bytes 7379–7400

```text
the process is simple
```

## exposed_roles / removed

### 11: filler.unscoped-assurance

Slow to accept connections specifies the operational symptom for increasing the connection timeout. It is a conditional restriction on databases, not a bare quality endorsement. The matcher misreads a relative predicate as the whole sentence judgment.

Status: nonactionable. Full: none. Partial: none.

`sources/c02.md` bytes 13811–13886

```text
Raise `--connect-timeout` for databases that are slow to accept connections
```

## confirmation / removed

### 27: filler.unscoped-assurance

The way-the-flag-functions-is-simple assertion is the frozen unscoped simplicity judgment. Its removal is a lost full detection.

Status: actionable. Full: c04-d10. Partial: none.

`sources/c04.md` bytes 13518–13563

```text
The way the `--type` flag functions is simple
```

### 29: filler.unscoped-assurance

The setup-is-simple sentence is the frozen unscoped ease claim. Its removal loses a full detection even though later concrete setup instructions remain.

Status: actionable. Full: c04-d14. Partial: none.

`sources/c04.md` bytes 20046–20087

```text
Setting up a configuration file is simple
```
