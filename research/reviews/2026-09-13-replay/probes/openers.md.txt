Measured on PostgreSQL 17.10, the index options have no slot on the schema surface, and the compatible binary drops all of them too.

Measured on PostgreSQL 17 with the vector extension, a database holding these indexes inspects to a document that the same tool applies without changes.

Measured on PostgreSQL 18.6. A range type created with a custom difference function still holds that function when the declared attribute is removed.
