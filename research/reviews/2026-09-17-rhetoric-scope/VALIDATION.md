# Validation record

The measured runtime is `d5ef492c15c67fcdcf2a5a7883f7f21d12e0340a`.
Reports were replayed from a clean detached checkout of that commit. Subsequent
changes contain evidence, documentation and expected rule identities. No runtime
semantics changed after confirmation output review.

- Public-API regressions passed for information judgments, attribution, quoted
  subjects, concrete purposes, conditional and measured claims, nominal actors,
  relative clauses, protected identifiers, source mappings and rule policy.
- The compiled CLI regression passed in both profiles, including a source-bound
  diagnostic, its revision, reported speech and a technical form definition.
- The first full check stopped on two stale rule-version expectations and ten
  feature golden files. Exactly 26 `ruleset_hash` fields changed in those goldens;
  feature values and diagnostics did not change.
- The evidence validator and its 13 tests passed. Negative probes reject source
  drift, incomplete finding reviews, cross-page and incidental-length credit,
  control credit and promotion of post-diagnostic claims into frozen recall.
  Partial events retain partial status.

All required stages of `make check` passed. Policy, tidy, ordinary tests across
all modules, strict lint in six checked modules, 35 Bash scripts, mirror/SBOM
rejection probes and the report schema passed. The sandbox denied system resource
reporting, so `check-performance`, `check-research-cost` and `check-reproducible`
ran outside it and passed. Six Linux/macOS/Windows amd64/arm64 binaries reproduced
byte for byte on this host with cgo disabled.

CLI and MCP self-checking matched evidence for 1,031 documents; failure, rewrite
and malformed-input probes passed. The staged working-tree scan reported 823
findings and passed its gate. The new evidence documents add only three ARI
measurements in the immutable protocol; the technical wording is retained.
A final rebuild and CLI/MCP check follow the evidence commit. Race detection,
active fuzzing and coverage remain deferred under #123. No Docker resources
were created.

Local validation does not establish terminal remote CI, merging, deployment,
model qualification or satisfactory broad editorial recall.
