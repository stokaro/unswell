# Remaining confirmation defects

All 66 lack full coverage in both profiles. No partial event receives full credit.

## c02-d01: empty_framing

The understanding claim personifies the parser and overstates the observable parse/render result.

Proposed edit: State that rendering validates the parsed statements, retaining retargeting and failure limits.

Bytes 1643–1690

```text
it proves the parser
understood every statement
```

## c02-d03: needless_repetition

This restates the immediately preceding permissive-reader explanation without naming a new rule.

Proposed edit: Keep the permissive-reader behavior and mixed-convention consequence.

Bytes 4084–4119

```text
No dialect means no
dialect's rules
```

## c02-d04: vague_claims

The frequency claim about single-engine files lacks a stated population or evidence.

Proposed edit: Keep the conditional instruction to name the dialect for a single-engine file.

Bytes 4238–4260

```text
which is nearly always
```

## c02-d05: empty_framing

The paragraph announces the following section after already linking shared operations.

Proposed edit: Keep the shared-operations link and SQL flag; remove the announcement.

Bytes 4566–4606

```text
What follows is specific to this source.
```

## c02-d06: unjustified_intensifiers

The explicit refusal and exit diagnostic already convey visibility.

Proposed edit: Use refused and retain the exact failure example.

Bytes 6149–6155

```text
loudly
```

## c02-d07: wordiness

The level/lives-at metaphor repeats structural ownership while delaying the actual unsupported default-name behavior.

Proposed edit: Say these names belong to table constraints, while supported engines do not record default constraint names.

Bytes 6697–6762

```text
which is the level a name lives at. A
  default has no such level
```

## c02-d08: wordiness

The nowhere-to-live and not-one-any-engine-would-run layers obscure the missing table requirement.

Proposed edit: Say the primary-key declaration requires its table in the same schema file, then retain both repair options.

Bytes 9964–10075

```text
A primary key has nowhere to live without its table, and the document is not
  one any engine would run either.
```

## c02-d09: wordiness

The text-nothing-read and no-place layers repeat why an unparsed routine cannot participate in comparison.

Proposed edit: Say an unparsed routine body cannot be compared as desired state; retain the accepted body forms.

Bytes 10376–10458

```text
and text nothing read cannot be compared, so it has no place in a
  desired schema
```

## c03-d01: wordiness

The positive and negative completeness restatement repeats that the index is generated from the command tree.

Proposed edit: Say the generated index lists all registered command paths; keep ordering and description provenance.

Bytes 1458–1534

```text
so it names
every command path the binary answers to and no path it does not
```

## c03-d02: empty_framing

The attention announcement adds nothing to the following specific misleading-success behavior.

Proposed edit: Keep the two exit-0 paths and the prescribed output check.

Bytes 8241–8282

```text
and they are the ones worth knowing about
```

## c03-d03: needless_repetition

The final sentence repeats the already specified verified covered file set.

Proposed edit: Keep the layout-specific coverage rules and checksum ordering.

Bytes 17430–17499

```text
What executes is what the verified checksum covers, for every layout.
```

## c03-d04: empty_framing

The broad contract judgment justifies presentation rather than specifying the native output difference.

Proposed edit: Keep the native twin and distinct output format.

Bytes 21749–21819

```text
only the compatibility surface is a contract with an existing
pipeline
```

## c03-d05: wordiness

The note-exists and what-does/not layers obscure the concrete diagnostic difference.

Proposed edit: Say Ptah preserves Atlas query behavior but warns about ignored keys; keep the typo example and strict option.

Bytes 26957–27051

```text
The note exists because
dropping the key is what Atlas does and saying nothing about it is not
```

## c03-d06: unjustified_intensifiers

Visibility emphasis adds no separate refusal condition.

Proposed edit: Introduce the refused inputs directly.

Bytes 27769–27787

```text
all of them loudly
```

## c03-d07: empty_framing

The gap metaphor repeats the explicitly documented empty-string representation.

Proposed edit: Keep the exact output spelling and both locations.

Bytes 37367–37430

```text
so the present token is not an anonymous
gap in operator output
```

## c03-d08: empty_framing

The intent endorsement adds no accepted spelling or compatibility fact.

Proposed edit: Keep the refusal, supported spelling and CE scope.

Bytes 53435–53500

```text
refusing an unimplemented spelling loudly is the intended
outcome
```

## c03-d09: wordiness

The document-means-what-the-database-holds wrapper obscures semantic preservation.

Proposed edit: Say removing a field must preserve the represented schema.

Bytes 60348–60405

```text
still write a document that means what the database holds
```

## c03-d11: needless_repetition

The circular identities repeat the type/comment examples without clarifying their semantics.

Proposed edit: Say a column requires a type, while a comment can be removed without changing schema behavior.

Bytes 60545–60643

```text
because a column without one is not
a column. A comment can go, and the table is still that table.
```

## c03-d12: needless_repetition

The identity repeats the explicit presence of both flags without adding their precedence.

Proposed edit: State that supplying both flags is rejected regardless of the environment variable.

Bytes 64997–65035

```text
Typing both flags is
still typing both
```

## c03-d13: wordiness

The no-shape/choose/chosen-to-match chain repeats the reason for selecting the sibling output contract.

Proposed edit: Say the unavailable Atlas payload leaves Ptah free to use the schema-diff field names; retain provenance limits.

Bytes 76633–76749

```text
so there was no shape to match — only one to choose and write down.
What it is chosen to match is the sibling verb
```

## c03-d14: empty_framing

The judgment about operator choice repeats the leading-comment-block placement rule.

Proposed edit: Keep the recognized header boundary and ignored later directive behavior.

Bytes 78730–78787

```text
which is why the position is not the operator's to
choose
```

## c03-d15: empty_framing

The purpose endorsement repeats the already stated replay/end-state check.

Proposed edit: Keep the fingerprint limitation and mandatory replay, removing the endorsement.

Bytes 82881–82945

```text
and the second question is the one this command exists
to answer
```

## c03-d16: wordiness

The nested report/plan/transition/change explanation repeats the need to validate the plan first.

Proposed edit: Say validation ensures the lint report describes the requested transition; preserve both checks.

Bytes 84743–84870

```text
a report about a plan that does not describe this transition would be
an accurate report about a change nobody is about to make
```

## c03-d17: empty_framing

The reviewer/review/approval defense obscures the explicit opt-in gate and coverage disclaimer.

Proposed edit: Keep the default exit behavior, opt-in gate and coverage disclaimer; introduce the two policies directly.

Bytes 85377–85598

```text
A plan is a document an operator reviews and
approves, the lint report is what they review it with, and a report that
refuses on their behalf is one they cannot approve anything with. Both of the
following are deliberate:
```

## c03-d18: empty_framing

The meaning endorsement adds no ordering requirement.

Proposed edit: Keep the requirement to establish the starting state before apply.

Bytes 89333–89373

```text
which is what makes a case mean
anything
```

## c03-d19: empty_framing

The emphatic purpose clause can be replaced by the actual test consequence.

Proposed edit: Say recomputing would hide a saved plan that no longer matches the planner; keep execution of the saved statements.

Bytes 90042–90085

```text
is precisely what this verb
exists to catch
```

## c03-d20: needless_repetition

The closing historical defense repeats the default gate and coverage policy already given in the same sub-verb section.

Proposed edit: Remove the repeated defense or replace it with a short link to the gate and coverage policy.

Bytes 90520–91212

```text
`lint` was deferred alongside it for a different reason, and that reason is
answered rather than outstanding. The worry was a linter in a gating position
reporting clean on a plan a wider rule set would flag. Ptah's answer is to take
the gate out of the default and put the coverage on the output: the verb
reports, the exit code is not a verdict unless a pipeline asks for one with
`PTAH_ATLAS_PLAN_LINT_FAIL_ON_ERROR`, and every run says on stderr that a
report without findings describes the rules rather than the plan. The analyzer
coverage itself is not private to this verb — it is the published set in
[Lint rules](../lint-rules/), reported identically by `ptah-compat migrate
lint`.
```

## c04-d01: wordiness

Designed-to-allow-to-use adds layers around library reuse without a named design constraint.

Proposed edit: Say other applications can use the library; do not claim they actually do.

Bytes 1449–1511

```text
the library is designed to allow other applications to
 use it
```

## c04-d02: wordiness

The want-to-remain/functional-and-buildable wording lengthens the support objective.

Proposed edit: State that the project aims to support the listed and later tool versions; retain the older-version caveat.

Bytes 2307–2383

```text
we
 want it to remain functional and buildable with these and later versions
```

## c04-d03: empty_framing

The metaphor supplies no portability constraint or action.

Proposed edit: Keep the multi-platform reminder and specify unsupported assumptions if known.

Bytes 5254–5277

```text
Don't walk on the edge!
```

## c04-d04: vague_claims

The universal ease judgment has no reader or complexity criterion.

Proposed edit: Keep the entry-point and file-location facts; remove the ease claim.

Bytes 5568–5628

```text
All of those functions are
 rather small and easy-to-follow.
```

## c04-d05: vague_claims

One replacement-function mechanism does not establish absolute platform independence.

Proposed edit: Say the replacement functions handle platform differences in printf behavior.

Bytes 6084–6141

```text
This
 makes sure we stay absolutely platform independent.
```

## c04-d06: unjustified_intensifiers

The listed call sequence needs no approximation filler.

Proposed edit: Introduce the call sequence directly.

Bytes 6864–6873

```text
basically
```

## c04-d07: wordiness

Important and key duplicate emphasis without distinguishing function roles.

Proposed edit: Say key functions and retain when they are called.

Bytes 7037–7065

```text
most important key functions
```

## c04-d08: unjustified_intensifiers

The assumed reader knowledge adds no protocol behavior.

Proposed edit: State the protocol-specific negotiation and setup directly.

Bytes 8221–8230

```text
of course
```

## c04-d09: needless_repetition

This circular sentence repeats the preceding post-transfer timing and adds no cleanup action.

Proposed edit: Keep the timing, reuse preparation and possible disconnect.

Bytes 9336–9415

```text
This function takes care of everything
   that has to be done after a transfer.
```

## c04-d10: empty_framing

The interest announcement delays the actual request-buffer implementation.

Proposed edit: Name the buffering functions directly; retain single-write behavior and reason.

Bytes 11022–11068

```text
An interesting detail with the HTTP(S) request
```

## c04-d11: unjustified_intensifiers

The compliment adds no environment-variable or allocation semantics.

Proposed edit: Keep platform independence and the allocated-buffer difference.

Bytes 12860–12864

```text
neat
```

## c04-d12: unjustified_intensifiers

The reader-knowledge assumption adds no closure condition.

Proposed edit: Retain all option and protocol closure conditions.

Bytes 14509–14518

```text
of course
```

## c04-d13: unjustified_intensifiers

The assumption adds no multi-interface ownership condition.

Proposed edit: Keep the ownership exception.

Bytes 14611–14620

```text
of course
```

## c04-d14: unjustified_intensifiers

Nonblocking is the operative property; the intensifier supplies no extra criterion.

Proposed edit: Say nonblocking and retain the name-resolution scope.

Bytes 15183–15192

```text
perfectly
```

## c04-d15: wordiness

The two verbs repeat the same code change here.

Proposed edit: Use adapt and retain state-machine and protocol distinctions.

Bytes 15275–15291

```text
adapt and adjust
```

## c04-d16: vague_claims

The dismissive evaluation does not identify the implementation problem.

Proposed edit: Describe the blocking behavior or rewrite need without the insult.

Bytes 15527–15542

```text
crappy examples
```

## c04-d17: empty_framing

The optimality preface gives no measurable API property.

Proposed edit: Begin with the generic SSL API and required use.

Bytes 15950–16003

```text
To deal with this internally in the best way possible
```

## c04-d18: empty_framing

The self-assessment adds no return-code behavior.

Proposed edit: Begin with the return-code contract.

Bytes 16929–16953

```text
I've made things simple.
```

## c04-d19: wordiness

The overlapping export/show/internals phrases can be consolidated without losing the encapsulation goal.

Proposed edit: Say the project tries to keep implementation details out of the public API; retain the stability rationale.

Bytes 17634–17684

```text
not export or show internals or how internals work
```

## c04-d20: empty_framing

The simplicity endorsement does not describe the wrappers.

Proposed edit: Begin with normal operation followed by logging.

Bytes 19023–19076

```text
The functions in the memdebug system do nothing fancy
```

## c04-d21: wordiness

The narrative action wrapper adds no installation step.

Proposed edit: Next, I added the include path and library; preserve the retrospective context.

Bytes 21751–21779

```text
Next thing I did was I added
```

## c04-d22: wordiness

The notice wrapper delays the threading limitation.

Proposed edit: State the single-thread limitation directly.

Bytes 28491–28507

```text
Please note that
```

## c04-d23: wordiness

This assurance wrapper adds no verification separate from the rebuild step.

Proposed edit: Rebuild the program and link it with the instrumented library.

Bytes 29333–29367

```text
Make sure you rebuild your program
```

## c04-d24: vague_claims

The quality endorsement can be replaced by the actual suggested action.

Proposed edit: Suggest posting the report to the mailing list.

Bytes 29899–29948

```text
This report is very fine for posting to the list!
```

## c04-d25: unjustified_intensifiers

The assumed obviousness adds no backward-compatibility behavior.

Proposed edit: Keep the compatibility statement and callback mechanism.

Bytes 31569–31578

```text
Of course
```

## c04-d26: empty_framing

The vague importance judgment repeats the specific stream/connection ownership explanation.

Proposed edit: Keep the exact stream and shared-connection relationship.

Bytes 34347–34438

```text
Multiplexing
  makes it even more important to keep things associated with the right thing!
```

## c04-d27: wordiness

Keep-around/after-use/used-again/reuse layers repeat connection caching.

Proposed edit: Say libcurl caches reusable connections to avoid creating new ones; preserve that reuse is conditional.

Bytes 34483–34733

```text
  A general idea in libcurl is to keep connections around in a connection
  "cache" after they have been used in case they will be used again and then
  re-use an existing one instead of creating a new as it creates a significant
  performance boost.
```

## c04-d28: empty_framing

The conclusion repeats that the easy API wraps the multi API.

Proposed edit: Keep the wrapper relationship.

Bytes 36305–36343

```text
This makes everything multi interface.
```

## c05-d01: needless_repetition

This sentence repeats the general UML mechanisms introduction without new member syntax.

Proposed edit: Remove the repeated setup and begin with attributes versus methods.

Bytes 3263–3385

```text
UML provides mechanisms to represent class members, such as attributes and methods, and additional information about them.
```

## c05-d02: wordiness

The repeated two-ways announcement and regardless-of-whichever-syntax clause obscure a simple equivalence.

Proposed edit: Say either of the following two syntaxes produces the same members.

Bytes 3587–3765

```text
There are two ways to define the members of a class, and regardless of whichever syntax is used to define the members, the output will still be same. The two different ways are :
```

## c05-d03: wordiness

This repeats the immediately preceding bracket instruction.

Proposed edit: Keep the grouped-member syntax and multiple-member use case.

Bytes 4295–4342

```text
where members are grouped within curly brackets
```

## c05-d04: needless_repetition

The standalone return-type sentence repeats the preceding Return Type section and adds no generic-specific instruction.

Proposed edit: Keep one return-type section with spacing and generic examples.

Bytes 6056–6150

```text
Optionally you can end the method/function definition with the data type that will be returned
```

## c05-d05: wordiness

May-be-placed and optional repeat modality around the same instruction.

Proposed edit: Optionally prefix the member name with a visibility marker.

Bytes 6245–6319

```text
these notations may be placed before the member's name, but it is optional
```

## c05-d06: wordiness

Also and as well repeat addition, while can-be-used lengthens the directional syntax.

Proposed edit: Arrowheads can also point in the opposite direction; retain the example.

Bytes 7608–7667

```text
Also, arrowheads can be used in opposite directions as well
```

## c05-d08: unjustified_intensifiers

The ease claim adds no placement or quoting requirement.

Proposed edit: Keep optional placement and quoted cardinality syntax.

Bytes 9126–9132

```text
easily
```

## c05-d09: wordiness

It-is-possible-to, specific-marker-text and clear-indication-of-nature layer vague descriptions over class annotations.

Proposed edit: Say class annotations identify interfaces, abstract classes, services or enums; retain the examples.

Bytes 9601–9789

```text
It is possible to annotate classes with a specific marker text which is like meta-data for the class, giving a clear indication about its nature. Some common annotations examples could be:
```

## c05-d10: wordiness

The announcement repeats both the count and syntax equivalence.

Proposed edit: Introduce the two equivalent annotation syntaxes once.

Bytes 10042–10166

```text
There are two ways to add an annotation to a class and regardless of the syntax used output will be same. The two ways are :
```

## c06-d01: unjustified_intensifiers

The assumed obviousness adds no nested-selection behavior.

Proposed edit: State that the same syntax applies at any depth.

Bytes 6392–6401

```text
Of course
```

## c06-d02: empty_framing

The caution announces a need for care rather than stating the following exact integer-key requirement.

Proposed edit: Begin with the integer-key requirement for lists and tuples; retain the all-elements alternative.

Bytes 6445–6557

```text
Special care must be taken when including or excluding fields from a list or tuple of submodels or dictionaries.
```
