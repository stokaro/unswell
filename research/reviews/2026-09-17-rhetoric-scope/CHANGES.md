# Reviewed changes

Indices refer to the retained strict reports.

## exposed_local / added

### 7: filler.evaluative-closure

The complete whole-value announcement is now identified before the colon. Later operational says no longer suppresses it; preserve the actual credential behavior.

Status: actionable. Full: c02-d02. Partial: none.

`sources/c02.md` bytes 4113–4160

```text
That distinction is the whole value of the verb
```

## exposed_context / removed

### 49: filler.instruction-scaffolding

This is an imperative release-note entry describing a newly added capability, not an actor followed by a redundant support chain. The parser treats the preceding issue-link residue as an actor; this is a false match.

Status: nonactionable. Full: none. Partial: none.

`sources/c11.md` bytes 16695–16823

```text
#2848](https://github.com/moment/moment/pull/2848) Allow user to get/set the rounding method used when calculating relative time
```

## exposed_instruction / removed

### 129: filler.instruction-scaffolding

Allows a user to upload defines the form being demonstrated in HTML. It is a restrictive relative clause inside an example introduction, not a standalone reader instruction. The proposed direct-action interpretation is inappropriate.

Status: nonactionable. Full: none. Partial: none.

`sources/c08.md` bytes 11739–11817

```text
A form that
 allows a user to upload a file could be written like this in HTML
```

## exposed_projection / added

### 7: filler.evaluative-closure

One source-bound warning identifies both the whole-guarantee and cognitive-worth announcements. The actual atomic commit and rollback behavior remains outside the span.

Status: actionable. Full: c02-d02. Partial: none.

`sources/c02.md` bytes 1637–1699

```text
That is the whole guarantee, and it is worth stating precisely
```

### 9: filler.evaluative-closure

The quoted phrase is the subject being evaluated, not a quotation of the judgment. Reading-twice advice matches the frozen defect; keep all feeder and retention conditions.

Status: actionable. Full: c02-d05. Partial: none.

`sources/c02.md` bytes 2365–2423

```text
"Every usable live feeder" is the part worth reading twice
```

### 12: filler.evaluative-closure

The importance announcement names an audience but no embedding consequence. Keep the specific consequences that follow.

Status: actionable. Full: c03-d01. Partial: none.

`sources/c03.md` bytes 4368–4405

```text
That separation matters for embedders
```

## exposed_projection / removed

### 7: filler.evaluative-closure

The cognitive-worth phrase identifies part of c02-d02; the preceding whole-guarantee announcement remains outside the diagnostic. Do not claim full coverage.

Status: actionable. Full: none. Partial: c02-d02.

`sources/c02.md` bytes 1670–1699

```text
it is worth stating precisely
```

## confirmation / added

### 33: filler.evaluative-closure

The whole-criterion announcement matches c03-d10; retain the actual subtractable-field definition and examples.

Status: actionable. Full: c03-d10. Partial: none.

`sources/c03.md` bytes 60407–60434

```text
That is the whole
criterion
```
